# Implementation Plan: Issue-Driven Self-Improvement Loop (REQ-000057)

## Plan Metadata

| Field | Value |
|---|---|
| Title | Issue-Driven Self-Improvement Loop — Implementation Plan |
| Linked TDD | `docs/tdd/REQ-000057-req-000057.md` (v1.0, Draft — TDD gate passed) |
| Linked PRD | `docs/prd/REQ-000057-req-000057.md` (v1.0) |
| Tracking Issues | #619 (primary), related #620 (REQ-000056), #617 |
| Author | Plan Author Agent (pwatsonr@gmail.com) |
| Date | 2026-07-01 |
| Version | 1.0 |
| Status | Draft — pending plan review |
| Target Repo | `/Users/pwatson/codebase/autonomous-dev` |
| Total Estimated Effort | **56.0 hours** (48h + 20% buffer ≈ 8 dev-days; TDD rough 11d for pairs; plan buffers task-level) |
| Critical Path Length | **9 sequential tasks**, ~28.8 hours end-to-end |
| Parallel Tracks | **4 tracks** (A: foundation/ledger, B: catalog/description, C: gh + evidence, D: gate + events + docs) |

**Scale check.** This is a genuinely complex change (new module tree, watch-tick integration, CLI surface, persistent ledger, merge-gate invariant, schemas, and E2E tests). Full decomposition with dependency graph, parallel tracks, and per-task risk assessment is warranted. See §3–§5.

---

## 1. Task List

Each task is 1–4 hours of executor effort, with a clear acceptance boundary and verification commands. Effort estimates include test authoring (~30–50% of task budget) and a 20% buffer for unforeseen complications.

Repeated verification commands (unless a task overrides them):

- Lint: `cd /Users/pwatson/codebase/autonomous-dev/plugins/autonomous-dev && npm run lint`
- Type-check: `cd /Users/pwatson/codebase/autonomous-dev/plugins/autonomous-dev && npx tsc --noEmit`
- Unit tests (module scope): `cd /Users/pwatson/codebase/autonomous-dev/plugins/autonomous-dev && npx jest intake/triggers/self_improve/__tests__/<test file>`
- Full unit test suite: `cd /Users/pwatson/codebase/autonomous-dev/plugins/autonomous-dev && npx jest`
- Bats regression (task T9 only): `cd /Users/pwatson/codebase/autonomous-dev/plugins/autonomous-dev && bats tests/self-improve-no-auto-merge.bats`

---

### TASK-001 — Config module

- **ID**: TASK-001
- **Title**: `self_improve/config.ts` + env parser
- **Description**: Implement `SelfImproveConfig` shape and `readSelfImproveConfig(env)` per TDD §4.6. Fail-safe defaults for every field; malformed env value falls back to default AND emits a deferred `self_improve_config_invalid` event carrier (event schema itself lands in TASK-011). For this task, expose a `configWarnings: ConfigWarning[]` field on the returned object; the emitter wires it up later.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/config.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/config.test.ts` (new)
- **Dependencies**: none
- **Acceptance Criteria**:
  1. All env-var names match the PRD §7 table verbatim (`AUTONOMOUS_DEV_SELF_IMPROVE`, `..._MAX_ATTEMPTS`, `..._MAX_CONCURRENT`, `..._MAX_CONCURRENT_PER_REPO`, `..._MAX_COST_USD_PER_DAY`, `..._MAX_COST_USD_PER_WEEK`, `..._BACKOFF_BASE_MINUTES`, `..._FN_REGISTRY_PATH`, `..._MAX_ISSUES_PER_TICK`, `..._EVIDENCE_TIMEOUT_MS`, `..._BODY_TRUNCATE_BYTES`, `AUTONOMOUS_DEV_BOT_LOGIN`).
  2. `enabled === (env.AUTONOMOUS_DEV_SELF_IMPROVE === '1')` — no truthy-string coercion.
  3. Defaults match §4.6 table exactly (attempts=3, concurrent-global=2, per-repo=1, daily=5.00, weekly=25.00, backoff=60, tick=5, evidence-timeout=500, truncate=32768).
  4. Any parse failure yields default value and a `configWarnings` entry with `{ envVar, raw, fallback }`.
  5. Missing `AUTONOMOUS_DEV_BOT_LOGIN` returns `botLogin: ''` (empty), NOT throw — degradation path handled downstream.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/config.test.ts`
- **Estimated Effort**: 2.0 h
- **Track**: A
- **Risks**: Low — pure env parsing.

---

### TASK-002 — Label constants

- **ID**: TASK-002
- **Title**: `self_improve/labels.ts` + regex helpers
- **Description**: Implement label constants and taxonomy per TDD §4.1. Add `parsePriorityLabel(labels): 'P0'|'P1'|'P2'|'P3'|null` and `parseTypeLabel(labels): 'bug'|'refactor'|null` helpers.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/labels.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/labels.test.ts` (new)
- **Dependencies**: none
- **Acceptance Criteria**:
  1. Exports match TDD §4.1 verbatim (`LABEL_PIPELINE_FAILED`, `LABEL_REVIEWER_FINDING`, `LABEL_AUTO_FIX`, `LABEL_SELF_FIX_PR`, `LABEL_IN_PROGRESS`, `DETECTED_LABELS`, `PRIORITY_LABEL_RE`, `TYPE_LABEL_RE`).
  2. `parsePriorityLabel(['autodev/priority:P0'])` → `'P0'`; unknown label → `null`; multiple priority labels → highest severity (P0 > P1 > P2 > P3).
  3. `parseTypeLabel(['autodev/type:refactor'])` → `'refactor'`; missing → `null`.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/labels.test.ts`
- **Estimated Effort**: 1.0 h
- **Track**: B
- **Risks**: Low.

---

### TASK-003 — Ledger schema, IO, reader, mutator

- **ID**: TASK-003
- **Title**: `self_improve/ledger.ts` — persistent state
- **Description**: Implement full ledger module per TDD §4.5 and §6.1: `LedgerFile` v1 schema, atomic write via `.tmp + rename` + `0o600` perms, `openSync('wx')` lock file with 500 ms retry + 60 s stale-lock recovery, corrupt-file preservation to sidecar (`ledger.json.corrupt-<ts>`), `LedgerReader` arithmetic (`countActiveGlobal`, `countActivePerRepo`, `costLast24h`, `costLast7d`), and `LedgerMutator` operations (`recordSubmission`, `recordOutcome`, `reset`, `reconcile`).
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/ledger.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/ledger.test.ts` (new)
- **Dependencies**: TASK-001 (config for windowing thresholds)
- **Acceptance Criteria**:
  1. Path resolves to `${io.homedir()}/.autonomous-dev/state/self-improve/ledger.json`; parent dir created `0o700`; file `0o600`.
  2. `saveLedger` uses `.tmp.<pid>.<rand>` + `rename`; concurrent writer via lock file is serialized (jest test simulates two writers).
  3. Corrupt JSON is preserved to sidecar with timestamp; in-memory state reset to empty; a warning is captured on the returned `LedgerFile` for later emission.
  4. `LedgerReader.costLast24h` iterates hour-bucketed `windowCosts` per OQ-5 (rolling window keyed by hour ISO strings).
  5. `reconcile()` accepts open-issue-key set; entries for absent keys marked `status: 'idle'` with `lastOutcome: 'unknown'` if not already set.
  6. Key format `${repoId}#${issueNumber}` validated against `SAFE_REPO_SLUG` + positive integer.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/ledger.test.ts`
- **Estimated Effort**: 4.0 h
- **Track**: A
- **Risks**: Medium — lock file semantics + stale-lock recovery are subtle. Mitigation: reference `trigger_store.ts` atomic-write pattern; add explicit stale-lock test.

---

### TASK-004 — Actionable catalog + classifier

- **ID**: TASK-004
- **Title**: `self_improve/actionable.ts` — data-driven detection
- **Description**: Implement `IssueSnapshot`, `IssueEventsSnapshot`, `ClassifierRow`, `ACTIONABLE_CATALOG` (A1/A2/A3), and `classify(issue, { botLogin })` per TDD §4.2.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/actionable.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/actionable.test.ts` (new)
- **Dependencies**: TASK-002 (labels)
- **Acceptance Criteria**:
  1. A1 matched when `LABEL_PIPELINE_FAILED` present AND (`authorLogin === botLogin` OR `fingerprint !== null`) AND body matches `<!-- autodev-failure: <hex>+ -->` regex.
  2. A2 matched when `LABEL_REVIEWER_FINDING` present AND body matches `<!-- autodev-reviewer: <id> -->` regex.
  3. A3 matched only when `LABEL_AUTO_FIX` present (human-labeler check deferred to evidence, per TDD §4.3).
  4. Rows evaluated in catalog order; first match wins; unmatched → `{ matched: null }`.
  5. Pure function; no I/O.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/actionable.test.ts`
- **Estimated Effort**: 2.0 h
- **Track**: B
- **Risks**: Low.

---

### TASK-005 — Evidence checks

- **ID**: TASK-005
- **Title**: `self_improve/evidence.ts` — confirmed-real-evidence
- **Description**: Implement `checkEvidence(klass, issue, ownership, deps)` per TDD §4.3 for A1/A2/A3. Timeout via `Promise.race(setTimeout)`; on timeout return `{ ok: false, reason: 'EVIDENCE_TIMEOUT' }` mapping to NA7 downstream.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/evidence.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/evidence.test.ts` (new)
- **Dependencies**: TASK-004
- **Acceptance Criteria**:
  1. A1: fingerprint marker parsed; `REQ-\d{6}` regex scans body for referenced request; `readState(repoPath, requestId).status === 'failed'` → `ok: true`. Missing request id but marker present → `ok: true` with `evidence.detail.mode === 'marker-only'`.
  2. A2: reviewer marker parsed; verdict must be `REQUEST_CHANGES`. Verdict `ERROR` → `ok: false, reason: 'NA7_REVIEWER_ERROR'`.
  3. A3: `fetchIssueEvents(repoId, issueNumber)` invoked; last `labeled` event with `LABEL_AUTO_FIX` inspected; `actor.login !== botLogin` → `ok: true`.
  4. Timeout of `evidenceTimeoutMs` default 500 ms enforced via race; timeout → `{ ok: false, reason: 'EVIDENCE_TIMEOUT' }`.
  5. All deps injected (no direct `fs`/`gh` calls in this module).
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/evidence.test.ts`
- **Estimated Effort**: 3.5 h
- **Track**: C
- **Risks**: Medium — `REQ-\d{6}` regex extraction from an arbitrary body is heuristic; mitigation: TDD documents "first matching" behavior; add fixture tests for zero-match, one-match, multi-match.

---

### TASK-006 — Guard pipeline

- **ID**: TASK-006
- **Title**: `self_improve/guards.ts` — GD1..GD12 + NA1..NA7
- **Description**: Implement `evaluateGuards(ctx)` per TDD §4.4. Ordering exactly `GD1 → GD2 → GD10 → GD9 → NA1 → GD4 → GD5 → GD6 → GD7 → NA3 → NA4 → NA2 → GD11 → (NA7 for A1/A2)`. First trip short-circuits with `{ ok: false, trip: { guard, evidence } }`. Also export `computeBackoffUntil(attempts, lastAttemptAt, baseMinutes)` with 24 h cap.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/guards.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/guards.test.ts` (new)
- **Dependencies**: TASK-001, TASK-003
- **Acceptance Criteria**:
  1. Table-driven test with one row per guard id (GD1..GD12 + NA1..NA7); each row asserts short-circuit at its guard.
  2. `computeBackoffUntil(1, T, 60)` → `T + 60 * 60_000`; `computeBackoffUntil(5, T, 60)` capped at `T + 24*60*60_000`.
  3. Pure function; no side effects.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/guards.test.ts`
- **Estimated Effort**: 3.5 h
- **Track**: A
- **Risks**: Medium — 19 guard ordering easy to miscode. Mitigation: match ordering to TDD §4.4 comment inline; table-driven tests catch reordering.

---

### TASK-007 — GitHub issue client

- **ID**: TASK-007
- **Title**: `self_improve/gh_issues.ts` — typed `gh` CLI wrapper
- **Description**: Implement `ghIssueClient(exec)` per TDD §4.10. Uses existing `ExecFn` from `intake/triggers/checks_client.ts`. Supports `listOpen(repoId, labels, limit)` with page-2 `Link: rel=next` follow-through; `getEvents`; `comment`.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/gh_issues.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/gh_issues.test.ts` (new)
- **Dependencies**: TASK-002 (labels)
- **Acceptance Criteria**:
  1. `listOpen('owner/name', DETECTED_LABELS, 100)` shells `gh api "/repos/owner/name/issues?state=open&per_page=100&labels=..."` via `ExecFn`; parses returned JSON into `IssueSnapshot[]`.
  2. When response body has ≥100 items, page 2 fetched; beyond page 2, iteration truncates and returns a `truncated: true` sentinel for the caller to emit `self_improve_issue_scan_truncated`.
  3. `getEvents` shells `gh api /repos/<repoId>/issues/<n>/events` and reduces to `IssueEventsSnapshot { labeledBy: Record<label, actor> }`.
  4. `comment` shells `gh issue comment <n> --repo <repoId> --body <body>`; `--body` passed as argv (not shell string) — no shell injection.
  5. All tests mock `ExecFn`; no real `gh` invoked.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/gh_issues.test.ts`
- **Estimated Effort**: 3.0 h
- **Track**: C
- **Risks**: Medium — page-2 pagination boundary. Mitigation: TDD §4.10 caps at page 2; explicit test for exactly-100, exactly-200, 201+ items.

---

### TASK-008 — Description builder

- **ID**: TASK-008
- **Title**: `self_improve/description.ts` — PRD §10 payload composer
- **Description**: Implement `buildSubmitPayload(issue, klass, cfg)` per TDD §4.7. Composition order: header line → source URL → fenced verbatim body (truncated to `bodyTruncateBytes`) → `## Acceptance Criteria` (extracted or synthesized) → constraints footer. Priority mapping P0/P1→high, P2→normal, P3→low. Type override for A3 via `autodev/type:*`.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/description.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/description.test.ts` (new)
- **Dependencies**: TASK-001, TASK-002, TASK-004
- **Acceptance Criteria**:
  1. Header line: `Auto-generated from ${repoId}#${n}: ${title}`.
  2. `## Acceptance Criteria` heading extraction: case-insensitive; bullet list up to next `##` heading captured as `acceptanceCriteria[]`.
  3. Fallback A: fenced ```test / ```expected block contents wrapped as criteria list items.
  4. Fallback B: synthesized `Reproduce the failure described above; verify the corresponding test/check now passes after the fix.`
  5. Body > `bodyTruncateBytes` (default 32 KB) truncated; `truncated: true` sentinel returned; original + truncated byte counts captured for later `self_improve_body_truncated` event.
  6. Priority mapping table verified: P0→high, P1→high, P2→normal, P3→low, default→normal.
  7. Type: A1/A2 default `bug`; A3 respects `autodev/type:refactor` override.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/description.test.ts`
- **Estimated Effort**: 3.0 h
- **Track**: B
- **Risks**: Medium — heading extraction has many edge cases; mitigation: three-tier fallback covers all inputs.

---

### TASK-009 — Events module + JSON schemas

- **ID**: TASK-009
- **Title**: `self_improve/events.ts` + `docs/schemas/events/*.schema.json`
- **Description**: Implement typed `EventEmitter` writing to the existing audit sink (`runWatchTick`'s `deps.audit`). Author five primary schemas + three auxiliary per TDD §5.3.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/events.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/events.test.ts` (new)
  - `docs/schemas/events/self_improve_issue_detected.schema.json` (new)
  - `docs/schemas/events/self_improve_issue_skipped.schema.json` (new)
  - `docs/schemas/events/self_improve_request_submitted.schema.json` (new)
  - `docs/schemas/events/self_improve_tick_summary.schema.json` (new)
  - `docs/schemas/events/self_improve_error.schema.json` (new)
  - `docs/schemas/events/self_improve_disabled.schema.json` (new)
  - `docs/schemas/events/self_improve_body_truncated.schema.json` (new)
  - `docs/schemas/events/self_improve_config_invalid.schema.json` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/events_schema.test.ts` (new — validates fixtures)
- **Dependencies**: TASK-001
- **Acceptance Criteria**:
  1. Every event type from TDD §5.3 has a schema.
  2. Every schema loads via `require` and validates a fixture example via `ajv` (existing dep — reuse from schema-validation tests if present, else jest table + hand-rolled shallow check).
  3. `EventEmitter` interface accepts `(ev: SelfImproveEvent) => void`; production wiring writes to existing audit stream.
  4. Timestamps auto-populated via injected `now()` if `ts` absent on caller-provided event.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/events*.test.ts`
- **Estimated Effort**: 3.0 h
- **Track**: D
- **Risks**: Low — schemas are additive.

---

### TASK-010 — Submit adapter

- **ID**: TASK-010
- **Title**: `self_improve/submit.ts` — `submitFromIssue` sequencing
- **Description**: Implement `submitFromIssue(issue, klass, cfg, deps)` per TDD §4.8. Sequence: `buildSubmitPayload` → `deps.requestSubmit(payload)` → on success `deps.ledger.recordSubmission(...)` (atomic write) → `deps.postGithubComment(...)` (best-effort) → `deps.emit(...)`. Router failure aborts pre-ledger; comment failure is logged but does NOT roll back ledger. Also do the minor export change on `intake/adapters/cli_adapter_entry.ts` to expose the `routerProvider` accessor (per TDD §4.11).
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/submit.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/submit.test.ts` (new)
  - `plugins/autonomous-dev/intake/adapters/cli_adapter_entry.ts` (modified — small additive export)
- **Dependencies**: TASK-003, TASK-008
- **Acceptance Criteria**:
  1. Successful submit → exactly one ledger write, then one comment call, then one `self_improve_request_submitted` event.
  2. Router throws → zero ledger writes, zero comments, one `self_improve_error` event with `code: 'SUBMIT_FAILED'`.
  3. Comment throws → ledger already committed; one `self_improve_error` event with `code: 'GH_COMMENT_FAILED'`; ledger row remains.
  4. `state.json` produced by router contains `source: 'self-improve'` and `self_improve.sourceIssue: { repoId, issueNumber, url, fingerprint }` (verified via router-fake introspection).
  5. `cli_adapter_entry.ts` export change is additive and does not break any existing consumer (existing tests remain green).
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/submit.test.ts` + full jest suite for `cli_adapter_entry` regression
- **Estimated Effort**: 3.5 h
- **Track**: A (join)
- **Risks**: High — router-provider export change touches the intake API surface. Mitigation: strictly additive export; TDD-OQ-A explicitly notes this. If router does not already support `source: 'self-improve'`, add as additive optional field (per TDD-OQ-A).

---

### TASK-011 — Merge-gate helpers + auto-merge call-site enforcement + bats regression

- **ID**: TASK-011
- **Title**: `self_improve/merge_gate.ts` + integration
- **Description**: Implement `isSelfImproveRequest(state)` and `checkAutoMergeAllowed(state, prLabels)` per TDD §4.9. Grep `bin/lib/` for any auto-merge helper and add a mandatory `checkAutoMergeAllowed` invocation at the top of each helper. Add bats regression `self-improve-no-auto-merge.bats` per TDD §8.3.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/merge_gate.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/merge_gate.test.ts` (new)
  - `plugins/autonomous-dev/tests/self-improve-no-auto-merge.bats` (new)
  - Auto-merge helper files under `plugins/autonomous-dev/bin/lib/` (modified — enumerated during task by grep)
- **Dependencies**: TASK-009 (event schema `self_improve_error` for enforcement log)
- **Acceptance Criteria**:
  1. `checkAutoMergeAllowed({ source: 'self-improve' }, [])` → `{ allow: false, reason: /never auto-merges/ }`.
  2. `checkAutoMergeAllowed({ source: 'self-improve' }, ['autodev:self-fix'])` → still `{ allow: false }` (self-improve invariant is state-based, not label-based; ADR-006).
  3. `checkAutoMergeAllowed({ source: 'user' }, ['autodev:self-fix'])` → `{ allow: true }` (or unchanged existing behavior).
  4. All auto-merge helpers found via `grep -R "auto-merge" plugins/autonomous-dev/bin/lib/` invoke `checkAutoMergeAllowed` before proceeding; any bypass fails a bats assertion.
  5. Bats regression: invokes an auto-merge helper with a self-improve fixture state.json; asserts non-zero exit + emitted audit event.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/merge_gate.test.ts` + `bats plugins/autonomous-dev/tests/self-improve-no-auto-merge.bats`
- **Estimated Effort**: 3.0 h
- **Track**: D
- **Risks**: High — if `bin/lib/` has zero auto-merge helpers, the enforcement is only preventative; document and file a follow-up. Mitigation: task explicitly performs the grep enumeration and records findings in the PR description.

---

### TASK-012 — Scan orchestrator

- **ID**: TASK-012
- **Title**: `self_improve/scan.ts` + E2E tests
- **Description**: Implement `scanEnrolledRepos(deps, now)` per TDD §4.13 data flow. Includes the `SelfImproveDeps` shape from TDD §5.2 and `buildDefaultSelfImproveDeps()` (returns `null` when disabled per TDD §5.2). E2E tests: `e2e_A1_happy_path`, `e2e_disabled`, `e2e_backoff_math`, `e2e_false_negative`, `e2e_tick_cap`, `e2e_body_truncated`.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/scan.ts` (new)
  - `plugins/autonomous-dev/intake/triggers/self_improve/index.ts` (new — public re-exports)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/scan.test.ts` (new)
- **Dependencies**: TASK-005, TASK-006, TASK-007, TASK-010, TASK-011 (merge-gate helpers exported through index)
- **Acceptance Criteria**:
  1. `GD1` off (`enabled=false`) → zero `gh` calls, one `self_improve_disabled` event.
  2. Enrollment check via injected `ownership`; unenrolled repo → NEVER queried (assertion + jest test).
  3. Global tick cap (`GD11`) applied across all repos, not per-repo.
  4. Reconciler pass runs BEFORE detection: for each ledger entry with `status: 'in_flight'`, `readRequestStatus` invoked; terminal outcomes update `windowCosts` and clear `in_flight`.
  5. Issue closed on GitHub → ledger entry marked `status: 'idle'`, `lastOutcome: 'unknown'` if not set; attempts NOT decremented.
  6. `NFR-RELIABILITY-01`: any thrown exception inside `scanEnrolledRepos` is caught at the top level and emitted as `self_improve_error`; never propagates out.
  7. Returns `{ scanned, submitted, skipped: Record<GuardId, number>, errors }`.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/scan.test.ts`
- **Estimated Effort**: 4.0 h
- **Track**: A (integration join)
- **Risks**: High — 6 E2E scenarios + reconciler + top-level exception handling. Mitigation: each E2E scenario written first as a failing test before wiring.

---

### TASK-013 — Watch-tick integration

- **ID**: TASK-013
- **Title**: `watch_tick.ts` — optional `selfImprove` dep
- **Description**: Add `selfImprove?: SelfImproveDeps` field to `WatchTickDeps`; add `selfImproveSubmitted: number` to `WatchTickResult`; invoke `scanEnrolledRepos` as a third phase after existing phases, guarded by `deps.selfImprove !== undefined` per TDD §4.12. Any thrown exception is caught and emitted; `runWatchTick` never crashes.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/watch_tick.ts` (modified)
  - `plugins/autonomous-dev/intake/triggers/__tests__/watch_tick.test.ts` (modified — add backward-compat + selfImprove tests)
- **Dependencies**: TASK-012
- **Acceptance Criteria**:
  1. Existing 11 tests in `watch_tick.test.ts` remain green with zero modification.
  2. New test `watch_tick omits selfImprove dep` asserts existing `WatchTickResult` shape returned with `selfImproveSubmitted: 0`.
  3. New test `watch_tick invokes selfImprove when present` asserts `scanEnrolledRepos` called exactly once with `deps.selfImprove`.
  4. New test `watch_tick catches selfImprove errors` asserts thrown error inside scan → `self_improve_error` emitted → `runWatchTick` still returns normally.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/__tests__/watch_tick.test.ts`
- **Estimated Effort**: 2.0 h
- **Track**: A (post-scan)
- **Risks**: High — regression risk on existing watch-tick tests. Mitigation: bit-for-bit additive design; test suite MUST pass before merge.

---

### TASK-014 — Reconciler polish + orphan handling

- **ID**: TASK-014
- **Title**: Reconciler edge cases in `scan.ts`
- **Description**: Complete reconciler behaviors deferred from TASK-012: (a) SUBMIT_PARTIAL orphan handling — if a request exists in intake DB but has no ledger entry, invoke `requestCancel(id, reason: 'self-improve-orphan')` per TDD §7.2 step 3; (b) unified `readRequestStatus` fake for tests; (c) additional `scan.test.ts` cases for orphan cancel + cost-window arithmetic.
- **Files**:
  - `plugins/autonomous-dev/intake/triggers/self_improve/scan.ts` (modified)
  - `plugins/autonomous-dev/intake/triggers/self_improve/__tests__/scan.test.ts` (modified — new cases)
- **Dependencies**: TASK-012
- **Acceptance Criteria**:
  1. Orphan detection: mock intake DB returns a `self-improve` request whose ledger key is absent → `requestCancel` invoked exactly once with reason `'self-improve-orphan'`; `self_improve_error` with `code: 'SUBMIT_PARTIAL'` emitted.
  2. Cost-window arithmetic: two terminal outcomes in the same hour bucket accumulate correctly; day rollover increments a new bucket.
  3. All existing TASK-012 tests still green.
- **Lint/Test Commands**: standard lint + `npx jest intake/triggers/self_improve/__tests__/scan.test.ts`
- **Estimated Effort**: 2.5 h
- **Track**: A
- **Risks**: Medium — `requestCancel` dep may need additive injection into `SelfImproveDeps`. Mitigation: additive optional field with fallback to no-op when unset.

---

### TASK-015 — CLI surface: `triggers-cli self-improve tick|status|reset`

- **ID**: TASK-015
- **Title**: `bin/triggers-cli.ts` — new verb group
- **Description**: Add `self-improve` verb group with three sub-verbs per TDD §4.13 and §5.4. Include `--help` and non-zero exit on unknown sub-verb (FR-CLI-04). `tick` invokes `scanEnrolledRepos` standalone. `status` prints tabular ledger dump (default) or JSON with `--format json`. `reset <repoId> <issueNumber>` clears the ledger entry via `LedgerMutator.reset`; unknown key → exit 1.
- **Files**:
  - `plugins/autonomous-dev/bin/triggers-cli.ts` (modified)
  - `plugins/autonomous-dev/bin/__tests__/triggers-cli-self-improve.test.ts` (new — subprocess invocations)
- **Dependencies**: TASK-012
- **Acceptance Criteria**:
  1. `triggers-cli self-improve tick` runs one scan pass and prints a summary of the returned `{ scanned, submitted, skipped, errors }`; exit 0.
  2. `triggers-cli self-improve status` prints columns `repoId | issueNumber | attempts | lastOutcome | backoffUntil | status`; `--format json` prints raw ledger.
  3. `triggers-cli self-improve reset owner/name 42` clears the entry; unknown key → exit 1 with `no ledger entry for owner/name#42; nothing to reset`.
  4. `triggers-cli self-improve unknown-verb` → exit 1 with usage line.
  5. Corrupt ledger → status prints `error: ledger unreadable; corrupt file preserved at <path>` and exits 2.
- **Lint/Test Commands**: standard lint + `npx jest bin/__tests__/triggers-cli-self-improve.test.ts`
- **Estimated Effort**: 3.0 h
- **Track**: D
- **Risks**: Medium — subprocess-based tests may be slow. Mitigation: keep to 3–5 subprocess invocations; unit-test the sub-command handlers directly.

---

### TASK-016 — Architecture doc + CLI docs + ADR persistence

- **ID**: TASK-016
- **Title**: Docs
- **Description**: Write `docs/architecture/self-improve-loop.md` (overview, gating model, event catalog, ledger schema, runbook for `reset`); update existing CLI docs to mention the new verb group; ensure ADR-001..007 from TDD §11 are committed alongside as anchor.
- **Files**:
  - `docs/architecture/self-improve-loop.md` (new)
  - `plugins/autonomous-dev/docs/cli/triggers.md` (modified if it exists; else create)
- **Dependencies**: TASK-001..TASK-015 (accuracy)
- **Acceptance Criteria**:
  1. Overview names the compound gate (`AUTONOMOUS_DEV_SELF_IMPROVE=1` + per-repo enrollment).
  2. Runbook covers: enabling loop, `self-improve status`, `self-improve reset`, false-negative-registry authoring.
  3. Event catalog lists all 8 event types with links to schemas.
  4. Links to TDD and PRD.
- **Lint/Test Commands**: `cd /Users/pwatson/codebase/autonomous-dev && test -f docs/architecture/self-improve-loop.md`
- **Estimated Effort**: 2.0 h
- **Track**: D
- **Risks**: Low.

---

## 2. Effort Roll-Up

| Task | Effort (h) | Track | Depends on |
|---|---|---|---|
| TASK-001 Config | 2.0 | A | — |
| TASK-002 Labels | 1.0 | B | — |
| TASK-003 Ledger | 4.0 | A | 001 |
| TASK-004 Actionable | 2.0 | B | 002 |
| TASK-005 Evidence | 3.5 | C | 004 |
| TASK-006 Guards | 3.5 | A | 001, 003 |
| TASK-007 gh_issues | 3.0 | C | 002 |
| TASK-008 Description | 3.0 | B | 001, 002, 004 |
| TASK-009 Events + schemas | 3.0 | D | 001 |
| TASK-010 Submit | 3.5 | A | 003, 008 |
| TASK-011 Merge-gate + bats | 3.0 | D | 009 |
| TASK-012 Scan | 4.0 | A | 005, 006, 007, 010, 011 |
| TASK-013 watch_tick | 2.0 | A | 012 |
| TASK-014 Reconciler polish | 2.5 | A | 012 |
| TASK-015 CLI | 3.0 | D | 012 |
| TASK-016 Docs | 2.0 | D | 001–015 |
| **Total (incl. buffer already in per-task estimates)** | **48.0 h** | | |

Overall plan effort with 20% headroom: **~56 h** (~7 focused dev-days). Roughly consistent with TDD §12 estimate of 11 rough-days (which was calibrated to pair-of-people or single-person including review latency).

---

## 3. Dependency Graph

Textual DAG (edges are `→`, meaning "required before"):

```
TASK-001 ─┬─▶ TASK-003 ─┬─▶ TASK-006 ─┐
          │             │             │
          ├─▶ TASK-006 ─┘             │
          ├─▶ TASK-008 ────┐          │
          ├─▶ TASK-009 ─▶ TASK-011 ─┐ │
          │                          │ │
          └─▶ (used by everything downstream)

TASK-002 ─┬─▶ TASK-004 ─┬─▶ TASK-005 ─┐
          │             ├─▶ TASK-008 ─┤
          ├─▶ TASK-007 ─────────────  │
          └─▶ TASK-008 ────────────┐  │
                                   │  │
TASK-003 ─▶ TASK-010 ◀─ TASK-008 ─┘  │
                    │                 │
                    └────▶ TASK-012 ◀─┴── TASK-005, TASK-006, TASK-007, TASK-011
                                 │
                                 ├─▶ TASK-013
                                 ├─▶ TASK-014
                                 └─▶ TASK-015

TASK-016 ◀── every task
```

**Critical path** (longest dependency chain):

`TASK-001 → TASK-003 → TASK-010 → TASK-012 → TASK-013 → TASK-016`
 2.0 h  +  4.0 h  +  3.5 h  +  4.0 h  +  2.0 h  +  2.0 h  =  **17.5 h**

An alternate near-critical path:
`TASK-002 → TASK-004 → TASK-005 → TASK-012 → TASK-014` = 1.0 + 2.0 + 3.5 + 4.0 + 2.5 = 13.0 h.

The join point is TASK-012 (scan orchestrator); any task feeding TASK-012 that finishes late lengthens the critical path.

---

## 4. Parallel Execution Schedule

Four tracks executing concurrently, joining at TASK-012 (integration).

```
Wave │ Track A (Foundation/Ledger) │ Track B (Catalog/Descr) │ Track C (gh + Evidence) │ Track D (Gate/Events/Docs)
─────┼─────────────────────────────┼─────────────────────────┼─────────────────────────┼───────────────────────────
  1  │ TASK-001 Config (2.0h)      │ TASK-002 Labels (1.0h)  │ (wait: 002)             │ (wait: 001)
  2  │ TASK-003 Ledger (4.0h)      │ TASK-004 Actionable (2h)│ TASK-007 gh (3.0h)      │ TASK-009 Events (3.0h)
  3  │ TASK-006 Guards (3.5h)      │ TASK-008 Description(3h)│ TASK-005 Evidence (3.5h)│ TASK-011 Merge (3.0h)
  4  │ TASK-010 Submit (3.5h)      │ (idle — merges to A)    │ (idle — merges to A)    │ (idle — merges to A)
  5  │ TASK-012 Scan (4.0h)  ◀── JOIN POINT: all upstream tasks must be green
  6  │ TASK-013 watch_tick (2.0h)  │                         │                         │ TASK-015 CLI (3.0h)
  7  │ TASK-014 Reconciler (2.5h)  │                         │                         │ (idle)
  8  │                             │                         │                         │ TASK-016 Docs (2.0h)
```

Wave-count end-to-end (assuming perfect parallelism, 4 executors): **~24 h wall-clock**. Single-executor sequential: **~48 h**.

Realistic pace with 2 executors (one on Track A, one on B/C/D interleaved): **~30 h wall-clock**.

---

## 5. Risk Register

Consolidated per-task risks with severities and mitigations:

| Risk | Severity | Related Task(s) | Mitigation |
|---|---|---|---|
| Ledger lock file stale-recovery misbehavior can cause tick starvation | Medium | TASK-003 | Explicit stale-lock jest test (mtime > 60 s); unit test both concurrent-writer and orphan-lock paths. |
| Guard ordering miscoded (19 guards, easy to swap) | Medium | TASK-006 | Ordering comment in `guards.ts` matches TDD §4.4 verbatim; table-driven test asserts short-circuit at each guard. |
| `REQ-\d{6}` extraction from arbitrary issue body has ambiguous multi-match cases | Medium | TASK-005 | TDD documents "first match" behavior; fixture tests for zero/one/multi. |
| `gh` pagination beyond page 2 truncates silently | Medium | TASK-007 | Emit `self_improve_issue_scan_truncated` sentinel to caller; scan test asserts emission. |
| Router-provider export change could destabilize `cli_adapter_entry.ts` consumers | **High** | TASK-010 | Strictly additive export; run full jest suite. Confirm TDD-OQ-A resolution during task. |
| `bin/lib/` may contain zero auto-merge helpers, leaving enforcement only preventative | **High** | TASK-011 | Grep enumeration recorded in PR description; if zero found, add a canonical helper stub + doc note. Bats regression still guarantees against future accidental additions. |
| Scan orchestrator has 6 E2E scenarios + reconciler + top-level exception handling — high complexity | **High** | TASK-012 | Each E2E written first as failing test (TDD-driven); bounded jest timeouts. Reconciler split into TASK-014 to reduce TASK-012 scope. |
| `watch_tick.ts` change could regress existing 11 tests | **High** | TASK-013 | Additive-only design; existing tests must be green with zero modification (acceptance criterion 1). |
| Subprocess-based CLI tests are slow and flaky | Medium | TASK-015 | Keep to 3–5 subprocess invocations; unit-test handlers directly. |
| Corrupt-ledger recovery on tick start (edge case): sidecar naming collision if two ticks both encounter corruption in the same second | Low | TASK-003 | Include monotonic counter suffix in sidecar filename (`ledger.json.corrupt-<isoTs>-<n>`). |
| `readRequestStatus` dep not yet exposed by intake DB | Medium | TASK-012, TASK-014 | Treat as additive optional dep on `SelfImproveDeps`; when absent, ledger `status` field is authoritative (per TDD §4.5). |
| TDD-OQ-C: A2 referenced-artifact format unresolved | Medium | TASK-005 | TDD defers to NA7 fallback; document; file follow-up for reviewer-chain PRD. |
| Bot-login env absent (OQ-1) disables A1 author fast path | Low | TASK-005 | Marker-only detection remains functional; documented behavior. |
| Cost-window rollover on hour/day boundary miscounted | Medium | TASK-014 | Hour-bucketed keys per OQ-5 (rolling window); explicit two-bucket cross-hour test. |

---

## 6. Traceability

Every TDD design element is mapped to at least one task:

| TDD Section | Design Element | Task(s) |
|---|---|---|
| §3.1 Module layout | Directory creation | TASK-001..TASK-016 (across the tree) |
| §4.1 `labels.ts` | Constants + regexes | TASK-002 |
| §4.2 `actionable.ts` | Data-driven catalog | TASK-004 |
| §4.3 `evidence.ts` | Confirmed-real-evidence | TASK-005 |
| §4.4 `guards.ts` | Ordered guard chain | TASK-006 |
| §4.5 `ledger.ts` | Persistent state | TASK-003 |
| §4.6 `config.ts` | Env parsing | TASK-001 |
| §4.7 `description.ts` | Payload composition | TASK-008 |
| §4.8 `submit.ts` | `submitFromIssue` sequencing | TASK-010 |
| §4.9 `merge_gate.ts` | Human-merge invariant | TASK-011 |
| §4.10 `gh_issues.ts` | Typed `gh` wrappers | TASK-007 |
| §4.11 Router wiring | `cli_adapter_entry.ts` export | TASK-010 |
| §4.12 `watch_tick.ts` change | Optional `selfImprove` dep | TASK-013 |
| §4.13 `triggers-cli.ts` change | `self-improve` verb group | TASK-015 |
| §5.3 Events + schemas | 8 event types + JSON schemas | TASK-009 |
| §5.5 `state.json` extension | Additive `source` + `self_improve` | TASK-010 |
| §6.1 Ledger file | Path + schema v1 | TASK-003 |
| §6.2 False-negative registry | Loader | TASK-001 (path) + TASK-006 (GD9 consumption) |
| §7 Error taxonomy | LOCK_BUSY, GH_*, SUBMIT_*, etc. | TASK-003, TASK-007, TASK-010, TASK-012 |
| §7.2 Reconciler | 3 reconciliation passes | TASK-012 (core) + TASK-014 (orphan) |
| §8 Testing strategy | Unit + E2E + bats + perf | Per-task test files + TASK-011 bats |
| §11 ADRs | ADR-001..007 | TASK-016 (published alongside docs) |

---

## 7. Blockers Requiring TDD/PRD Revision

No plan-time blockers. All open questions (OQ-1..OQ-10, TDD-OQ-A..C) are addressed in-plan with documented defaults per TDD §13. Spec phase should confirm:

1. **TDD-OQ-A** — Router-provider `source: 'self-improve'` first-class support (verified in TASK-010; additive field if needed).
2. **OQ-9** — Auto-merge call sites in `bin/lib/` enumerated during TASK-011.
3. **OQ-3** — A2 reviewer block format frozen during TASK-005 (uses TDD-proposed format).

If TASK-011 discovers zero auto-merge helpers exist, add a follow-up ticket to add a canonical `checkAutoMergeAllowed` invocation at the future auto-merge integration point; the bats regression still guards against accidental future auto-merge additions.

---

## 8. Definition of Done (plan-wide)

- All 16 tasks green (lint + type-check + jest per task).
- Full jest suite green: `cd plugins/autonomous-dev && npx jest`.
- Existing `watch_tick.test.ts` — 11 tests + new tests all green (TASK-013 regression gate).
- Bats regression `self-improve-no-auto-merge.bats` green.
- `AUTONOMOUS_DEV_SELF_IMPROVE=0` (default) → bit-for-bit-identical `runWatchTick` behavior (NFR-COMPAT-02 assertion in TASK-013).
- Docs published (TASK-016).
