# Implementation Plan — REQ-000054: Unblock autonomous merge/deploy path (#628 + #629)

| Field          | Value                                                              |
| -------------- | ------------------------------------------------------------------ |
| Request ID     | REQ-000054                                                         |
| Source TDD     | [docs/tdd/REQ-000054-req-000054.md](../tdd/REQ-000054-req-000054.md) |
| Author         | plan-author agent                                                  |
| Date           | 2026-06-30                                                         |
| Version        | 1.0                                                                |
| Status         | Draft (pending plan_review)                                        |

---

## 1. Plan Metadata

- **Title:** Unblock autonomous merge/deploy path — fix #628 (PR comment over-count) and #629 (merge gate over-strict).
- **TDD reference:** `docs/tdd/REQ-000054-req-000054.md` v1.0.
- **Total estimated effort:** **7.2h** engineer time (6.0h base + 20% buffer).
- **Critical path length:** **4 tasks / 3.6h** (TASK-001 → TASK-004 → TASK-005 → TASK-007 → TASK-008).
- **Parallel tracks:** **3** (Track A — merge-gate / #629; Track B — comment-loop / #628; Track C — docs).
- **Net code surface:** 1 production file (`bin/supervisor-loop.sh`), 1 config file (`config/defaults.json`), 3 bats files, 1 changelog entry.

---

## 2. Pre-flight Verification (executor MUST perform before TASK-001)

These five checks make sure the codebase matches the TDD's assumptions. Any deviation is a blocker and must be raised as plan_review feedback rather than worked around.

1. `plugins/autonomous-dev/bin/supervisor-loop.sh` exists and contains the functions named in TDD §14 (`maybe_merge_integration_pr`, `read_pr_comment_payload`, `pr_comment_new_ids`, `maybe_reenter_for_pr_comments`, `load_config`).
2. `plugins/autonomous-dev/config/defaults.json` exists and contains a top-level `daemon` object that already holds operational keys.
3. The three bats files referenced in TDD §7 exist: `tests/bats/pr_comment_loop.bats`, `tests/bats/trust_gated_merge.bats`, `tests/bats/merge_gate_order_aware.bats`, and each currently passes (`bats tests/bats/<file>` returns 0).
4. The `gh` stub harness used by those bats files is locatable (`grep -nE 'gh\(\)' tests/bats/*.bats` returns at least one stub definition or the helper file it sources).
5. Repository line numbers referenced in TDD §14 are within ±50 lines of the actual definitions — if the file has drifted heavily, executor should map by function name, not line number.

If any of these fail, write `status=fail` with the specific check that failed.

---

## 3. Task List

### TASK-001 — Add 3 new config keys + `load_config` wiring

- **Description:** Implement TDD §3.1. Add three keys under `daemon` in `config/defaults.json` (`merge_gate_non_blocking_checks`, `merge_gate_skip_baseline_red`, `pr_comment_non_actionable_authors`) with the defaults specified in the TDD. Wire `load_config` to export the corresponding shell globals (`MERGE_GATE_NON_BLOCKING_CHECKS`, `MERGE_GATE_SKIP_BASELINE_RED`, `PR_COMMENT_NON_ACTIONABLE_AUTHORS`).
- **Files:**
  - `plugins/autonomous-dev/config/defaults.json` (modify)
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (modify `load_config`, ~line 720–790)
  - `plugins/autonomous-dev/tests/bats/trust_gated_merge.bats` (add 1 new test: `load_config emits the three new globals with defaults`)
- **Dependencies:** none.
- **Acceptance criteria:**
  1. `jq '.daemon.merge_gate_non_blocking_checks' config/defaults.json` returns the 5-name list from TDD §3.1.
  2. `jq '.daemon.merge_gate_skip_baseline_red' config/defaults.json` returns `false`.
  3. `jq '.daemon.pr_comment_non_actionable_authors' config/defaults.json` returns the 5-substring list from TDD §3.1.
  4. New bats test verifying the three shell globals are non-empty after `load_config` passes.
  5. All existing bats files still pass.
- **Lint/Test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/trust_gated_merge.bats`
  - `jq empty plugins/autonomous-dev/config/defaults.json`
- **Estimated effort:** **0.6h** (0.5h base + 20% buffer).
- **Track:** A (foundational — gates all other tasks).
- **Risks:**
  - LOW: existing user `config.json` deep-merge semantics may union vs replace arrays. Verify by reading lines around 740 in `supervisor-loop.sh` and document outcome. Mitigation: existing TDD §4.3 states arrays are *replaced* — if not true, surface in plan_review.

---

### TASK-002 — Fix #628 root cause: PR-scope the `gh api` comments call

- **Description:** Implement TDD §3.3.2. In `read_pr_comment_payload` (~line 3760), replace the repo-wide `gh api repos/{owner}/{repo}/pulls/comments --paginate` call with a PR-scoped `gh api repos/{owner}/{repo}/pulls/${pr_number}/comments --paginate` call. Derive `pr_number` from `pr_url` with the exact `sed -nE` pattern in TDD §3.3.2. Fall back to `api_json="[]"` if the PR url cannot be parsed.
- **Files:**
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (modify `read_pr_comment_payload`)
  - `plugins/autonomous-dev/tests/bats/pr_comment_loop.bats` (add 2 tests: `gh api comments call is PR-scoped`, `PR url with no pull-number falls back gracefully`)
- **Dependencies:** TASK-001 (consumes globals only indirectly, but tests rely on `load_config` having run).
- **Acceptance criteria:**
  1. New bats test `gh api comments call is PR-scoped` passes — the recorded `gh` call log contains `/pulls/42/comments` and does **not** contain `/pulls/comments` (unscoped).
  2. New bats test `PR url with no pull-number falls back gracefully` passes — function returns valid JSON (`[]`) and does not exit non-zero.
  3. All existing tests in `pr_comment_loop.bats` still pass.
- **Lint/Test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/pr_comment_loop.bats`
- **Estimated effort:** **0.6h** (0.5h + 20%).
- **Track:** B (comment-loop / #628).
- **Risks:**
  - LOW: `pr_url` format may include trailing `/files` or `?diff=split`. The `sed` pattern uses `.*` greedy match — covered. Mitigation: TASK-002's malformed-URL test guards this.

---

### TASK-003 — Fix #628 follow-up: actionable-author filter + capture PR author

- **Description:** Implement TDD §3.3.3 and §3.3.4. Refactor `pr_comment_new_ids` (~line 3803) so it: (a) reads the full comment objects, not just IDs; (b) filters out comments whose `author.login` matches any substring in `PR_COMMENT_NON_ACTIONABLE_AUTHORS` (case-insensitive `contains`); (c) filters out comments whose `author.login == PR_AUTHOR_LOGIN`. Update `maybe_reenter_for_pr_comments` to capture the PR author via `gh pr view --json author --jq '.author.login'` and `export PR_AUTHOR_LOGIN` before calling `pr_comment_new_ids`. If `gh pr view --json author` fails, set `PR_AUTHOR_LOGIN=""` (self-filter disabled).
- **Files:**
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (modify `pr_comment_new_ids` and `maybe_reenter_for_pr_comments`)
  - `plugins/autonomous-dev/tests/bats/pr_comment_loop.bats` (add 5 tests per TDD §7.2: `bot authors are filtered out`, `PR author's own comments do not trigger re-entry`, `0 issue + 1 line comment from operator triggers exactly 1 re-entry`, `non-actionable filter is case-insensitive`, `filter respects config override`)
- **Dependencies:** TASK-001 (needs `PR_COMMENT_NON_ACTIONABLE_AUTHORS`), TASK-002 (needs PR-scoped payload).
- **Acceptance criteria:**
  1. All 5 new bats tests pass.
  2. The reproducer test (`0 issue + 1 line comment from operator triggers exactly 1 re-entry`) confirms `new_count == 1` and exactly 1 re-entry event is emitted.
  3. `_pr_comment_mark_addressed` continues to record every visible ID (idempotency invariant preserved — verify via test assertion on `addressed_ids[]` after a single tick).
  4. Existing tests in `pr_comment_loop.bats` still pass.
- **Lint/Test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/pr_comment_loop.bats`
- **Estimated effort:** **1.2h** (1.0h + 20%).
- **Track:** B.
- **Risks:**
  - MEDIUM: jq `contains` semantics on `null` author. Mitigation: the `jq` snippet in TDD §3.3.3 uses `(.author // "" | ascii_downcase)` — preserve that defensive default verbatim.
  - LOW: `export PR_AUTHOR_LOGIN` leaks into sibling processes. Mitigation: scope to the function or unset at end of `maybe_reenter_for_pr_comments`. Executor decides at implementation time but must add a test that confirms the global is not set permanently after the function returns (or that it's safe).

---

### TASK-004 — Add `_evaluate_merge_checks` helper + partition-logic unit tests

- **Description:** Implement TDD §3.2.1. Add a private helper `_evaluate_merge_checks(project, pr_url)` next to `_record_merge_decision` (~line 3395). The helper calls `gh pr checks <pr_url> --json name,state,bucket`, partitions checks into `failing` / `pending` / `ignorable` per the jq snippet in TDD §3.2.1, and prints `verdict<TAB>ignored_csv<TAB>blocking_csv`. Handle the `unreadable` case explicitly when `gh pr checks` returns empty or non-JSON.
- **Files:**
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (add new helper; do **not** yet wire it into `maybe_merge_integration_pr` — TASK-005 does that)
  - `plugins/autonomous-dev/tests/bats/trust_gated_merge.bats` (add unit-style tests that invoke `_evaluate_merge_checks` via the supervisor source-and-call pattern used by existing bats; assert ok/not_ok/unreadable verdicts with correct CSVs)
- **Dependencies:** TASK-001.
- **Acceptance criteria:**
  1. Helper returns `ok\t<allowlisted_failures>\t` when all failures are in the allowlist.
  2. Helper returns `not_ok\t<allowlisted_failures>\t<real_failures>` when ≥1 real check fails.
  3. Helper returns `not_ok` when ≥1 check is pending (`IN_PROGRESS`/`QUEUED`/`PENDING`).
  4. Helper returns `unreadable\t\t` on non-JSON / empty `gh pr checks` output.
  5. Helper always exits 0 (callers branch on verdict text).
- **Lint/Test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/trust_gated_merge.bats`
- **Estimated effort:** **1.2h** (1.0h + 20%).
- **Track:** A (merge-gate / #629).
- **Risks:**
  - MEDIUM: `gh pr checks --json` field set may differ across `gh` versions; `bucket` was introduced in a relatively recent release. Mitigation: the jq snippet already defensively defaults `(.bucket // "")` and falls back to `state`. The `unreadable` branch covers the worst case.
  - LOW: jq `IN(...)` filter requires jq ≥1.5. Mitigation: existing supervisor code already uses jq ≥1.6 idioms — no change to baseline.

---

### TASK-005 — Wire `_evaluate_merge_checks` into `maybe_merge_integration_pr`

- **Description:** Implement TDD §3.2.3. Replace the hard gate at ~line 3506 with the branching logic in TDD §3.2.3: (a) `mergeable != MERGEABLE` → existing `skip_not_mergeable` path; (b) `mergeStateStatus != CLEAN` AND allowlist empty AND baseline-red off → existing `skip_not_mergeable` (synthetic readiness explicitly disabled); (c) otherwise call `_evaluate_merge_checks` and branch on the verdict (`unreadable` → `skip_status_unreadable`, `not_ok` → `skip_not_mergeable` with blocking CSV in reason, `ok` → log info and fall through to existing G1/G2/G3/G4 gates and `gh pr merge --squash`). Implement TDD §3.2.4 audit-trail enrichment: when synthetic readiness was used, the `merged` `_record_merge_decision` reason includes the `ignored=[…]` list.
- **Files:**
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (modify `maybe_merge_integration_pr`)
  - `plugins/autonomous-dev/tests/bats/trust_gated_merge.bats` (add 4 tests per TDD §7.1: `merge gate ignores allowlisted failing checks`, `merge gate blocks when a non-allowlisted real check fails`, `merge gate blocks while real checks are still pending`, `merge gate handles unreadable gh pr checks`, `synthetic readiness disabled when allowlist empty AND baseline_red off`, `mergeable=CONFLICTING never proceeds to synthetic readiness`)
- **Dependencies:** TASK-004 (helper must exist).
- **Acceptance criteria:**
  1. All 6 new tests in `trust_gated_merge.bats` pass.
  2. The CONFLICTING test asserts `gh pr checks` is NOT called when `mergeable != MERGEABLE` (i.e. the helper short-circuits cleanly).
  3. A successful synthetic-readiness merge produces a `merge_decision` event with `reason` containing `synthetic-readiness, ignored=[…]`.
  4. Pre-existing trust-gated-merge tests still pass.
  5. The downstream G1/G2/G3/G4 gates are unchanged (verify by reading the file diff; no logic outside the gate block should change).
- **Lint/Test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/trust_gated_merge.bats`
- **Estimated effort:** **1.2h** (1.0h + 20%).
- **Track:** A.
- **Risks:**
  - MEDIUM: the existing skip path may call `_mark_pr_ready_for_human` with a string argument that downstream UI parses. Mitigation: keep the *first sentence* of the reason string identical to today's and append the synthetic-readiness annotation.
  - LOW: the `local allow_len=$(echo "$JSON" | jq 'length')` snippet runs jq once per merge attempt. Mitigation: trivial perf impact.

---

### TASK-006 — Implement `_baseline_red_checks` and opt-in plumbing

- **Description:** Implement TDD §3.2.2. Add `_baseline_red_checks(project, pr_url)` that resolves the base branch via `gh pr view ... --json baseRefName --jq '.baseRefName'`, then calls `gh api repos/{owner}/{repo}/commits/${base}/check-runs --jq '[ .check_runs[] | select(.conclusion=="failure") | .name ]'`, returning a JSON array. Best-effort: returns `[]` on any failure. Inside `_evaluate_merge_checks` (TASK-004), invoke `_baseline_red_checks` only when `MERGE_GATE_SKIP_BASELINE_RED == "true"` and merge its output into the `--argjson baseline` parameter as already designed in §3.2.1.
- **Files:**
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (add helper; small change inside `_evaluate_merge_checks` to consume it)
  - `plugins/autonomous-dev/tests/bats/trust_gated_merge.bats` (add 1 test: `baseline_red opt-in widens the allowlist`)
- **Dependencies:** TASK-004, TASK-005.
- **Acceptance criteria:**
  1. New test passes: with `MERGE_GATE_SKIP_BASELINE_RED=true`, a PR whose only failure is a check failing on the base branch HEAD is merged.
  2. With the flag false (default), the same PR is NOT merged.
  3. Failure of `gh api .../check-runs` returns `[]` (no widening, no crash).
- **Lint/Test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/trust_gated_merge.bats`
- **Estimated effort:** **1.2h** (1.0h + 20%).
- **Track:** A (parallelizable with TASK-007 once TASK-005 lands).
- **Risks:**
  - LOW: the `gh api` placeholder syntax `repos/{owner}/{repo}` only works inside a git checkout. Mitigation: the helper `cd`'s into `${project}` like every other `gh` call in the file.
  - LOW: `check-runs` returns only the latest run per check; older red runs are not surfaced. This is consistent with `gh pr checks` semantics; documented in TDD §11 Open Questions.

---

### TASK-007 — Order-aware composition tests (G1 rebase + G3 duplicate-patch)

- **Description:** Implement TDD §7.1 second table. Add tests in `tests/bats/merge_gate_order_aware.bats` verifying that (a) after a successful G1 rebase the new synthetic-readiness gate is *re-evaluated* (not skipped or cached), and (b) the G3 duplicate-patch gate still blocks a merged-twin PR even when synthetic readiness would otherwise pass.
- **Files:**
  - `plugins/autonomous-dev/tests/bats/merge_gate_order_aware.bats` (add 2 tests)
  - `plugins/autonomous-dev/bin/supervisor-loop.sh` (no production changes expected; if a test reveals a regression in gate ordering, raise as a blocker rather than altering production code unilaterally)
- **Dependencies:** TASK-005, TASK-006.
- **Acceptance criteria:**
  1. `synthetic readiness composes with G1 rebase` passes.
  2. `synthetic readiness still respects G3 duplicate-patch gate` passes.
  3. All pre-existing tests in `merge_gate_order_aware.bats` still pass.
- **Lint/Test commands:**
  - `bats plugins/autonomous-dev/tests/bats/merge_gate_order_aware.bats`
- **Estimated effort:** **0.6h** (0.5h + 20%).
- **Track:** A.
- **Risks:**
  - LOW: G1/G3 stub harness may need new mock entries. Mitigation: copy the existing pattern in the file; do not add new gh stub features.

---

### TASK-008 — Documentation: CHANGELOG entry + (optional) operator doc note

- **Description:** Add a `CHANGELOG.md` entry under the unreleased section that summarizes both fixes and points at #628 / #629. If `docs/operations/auto-merge.md` exists (executor verifies via Glob), add a 2-line note: "Auto-merge now accepts a configurable allowlist of non-blocking check names — see `config/defaults.json` keys `merge_gate_non_blocking_checks` and `merge_gate_skip_baseline_red`." If the file does not exist, skip the optional doc note and document the omission in the task notes.
- **Files:**
  - `CHANGELOG.md` (modify)
  - `docs/operations/auto-merge.md` (modify if present; otherwise skip)
- **Dependencies:** TASK-001 through TASK-007 (so the changelog describes the final shape).
- **Acceptance criteria:**
  1. `CHANGELOG.md` contains a new entry under the unreleased section referencing REQ-000054, #628, and #629.
  2. Markdown lints cleanly (`npx markdownlint CHANGELOG.md` if configured; otherwise visually verify no broken headings).
  3. If `docs/operations/auto-merge.md` exists, it contains the new 2-line note; otherwise the task notes record that the file is absent.
- **Lint/Test commands:**
  - `npx markdownlint-cli CHANGELOG.md` (best-effort; markdown lint is one of the "permanently red" gates so do not block on it)
- **Estimated effort:** **0.6h** (0.5h + 20%).
- **Track:** C (docs — parallel from TASK-005 onward; sequenced last so the entry reflects the final design).
- **Risks:**
  - LOW: markdown lint is itself a known-red gate (this is what we're working around). Mitigation: verify no NEW markdown lint regressions; existing failures stay.

---

## 4. Dependency Graph

```
TASK-001 (config + load_config)  [Track A foundation]
  ├──> TASK-002 (PR-scope comments)         [Track B]
  │       └──> TASK-003 (author filter)     [Track B]
  ├──> TASK-004 (_evaluate_merge_checks)    [Track A]
  │       └──> TASK-005 (wire into gate)    [Track A]
  │               ├──> TASK-006 (baseline-red)   [Track A]
  │               └──> TASK-007 (order-aware)    [Track A]
  └──> (TASK-008 docs depends on all above)
```

**Critical path:** `TASK-001 → TASK-004 → TASK-005 → TASK-007 → TASK-008` = 0.6 + 1.2 + 1.2 + 0.6 + 0.6 = **4.2h**.

**Note:** TASK-006 is *not* on the critical path because it runs in parallel with TASK-007 once TASK-005 is done.

No cycles. No tasks share files in a way that would cause merge conflicts when tracks are executed simultaneously (Track A and Track B both touch `supervisor-loop.sh` but in different functions — see §6 for the conflict mitigation).

---

## 5. Parallel Execution Schedule

```
Wall-clock hour ▶  0.0    0.6    1.2    1.8    2.4    3.0    3.6    4.2
Track A:           [001]──[004]──[005]──[006]──[007]──────────────────
Track B:                  [002]──[003]──────────────────────────────────
Track C:                                                      [008]────
```

- **t=0.0 → 0.6h:** TASK-001 runs alone (foundation).
- **t=0.6h → 1.8h:** Track A starts TASK-004; Track B starts TASK-002 (no file conflict — different function regions).
- **t=1.2h → 2.4h:** Track B continues with TASK-003; Track A continues with TASK-005.
- **t=2.4h → 3.6h:** Track A runs TASK-006 and TASK-007 in series (both touch the same gate path).
- **t=3.6h → 4.2h:** Track C runs TASK-008 (docs) — must follow Track A completion.

Wall-clock total with parallelism: **~4.2h**.
Single-threaded total: **7.2h** (sum of all task estimates incl. buffer).

---

## 6. File-Conflict Mitigation

`bin/supervisor-loop.sh` is touched by 6 of the 8 tasks. To prevent merge conflicts during parallel execution:

| Function region                  | Touched by         | Strategy                                                      |
| -------------------------------- | ------------------ | ------------------------------------------------------------- |
| `load_config` (~720–790)         | TASK-001           | Single-task region; no conflict.                              |
| `read_pr_comment_payload` (~3734–3797) | TASK-002       | Track B only.                                                 |
| `pr_comment_new_ids` (~3803–3812) | TASK-003          | Track B only; runs after TASK-002 on the same track.          |
| `maybe_reenter_for_pr_comments` (~3867–3998) | TASK-003 | Track B only.                                                 |
| `_evaluate_merge_checks` (NEW, near ~3395) | TASK-004, TASK-006 | TASK-006 modifies only the baseline-red branch; serialize on Track A. |
| `maybe_merge_integration_pr` (~3354–3688) | TASK-005   | Track A only; runs after TASK-004 completes.                  |

Track A and Track B never edit the same function. The executor should rebase Track B onto Track A before final integration.

---

## 7. Risk Assessment

| ID | Risk                                                                              | Severity | Mitigation                                                                                 |
| -- | --------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------ |
| R1 | `gh pr checks --json bucket` not supported on the deployment `gh` version          | MEDIUM   | Helper defensively defaults `.bucket // ""` and falls back to `.state`. Verify `gh --version` is ≥2.30 in the daemon environment as part of pre-flight. |
| R2 | Deep-merge of `config.json` over `defaults.json` unions arrays instead of replacing | MEDIUM   | TDD §4.3 asserts replace semantics. If verification (TASK-001) shows otherwise, raise as plan_review blocker so default-allowlist behavior is well understood. |
| R3 | `PR_AUTHOR_LOGIN` env var leaks into subsequent functions or supervisor ticks       | LOW      | Scope to the function (use `local` or explicit `unset` at function exit); add a test. |
| R4 | Markdown lint regression introduced by CHANGELOG entry                              | LOW      | Markdown lint is itself a "permanently red" allowlisted check (TDD §1). Acceptable not to block. |
| R5 | jq `IN(...)` not available on minimal jq builds                                     | LOW      | Existing supervisor code already uses jq ≥1.6 idioms; no change to baseline. |
| R6 | Test `gh` stub does not log the API path in a way TASK-002's assertion can read    | MEDIUM   | Read existing `tests/bats/pr_comment_loop.bats` `gh()` stub before writing the test. If the stub does not record paths, extend it minimally to write to `$GH_CALL_LOG`. |
| R7 | "Permanently red" markdown / visual-regression / scope-enforcement check names may differ from defaults | MEDIUM | Once TASK-001 lands, verify against `gh pr checks` on a recent PR in this repo. Adjust defaults if the actual names differ from those listed in TDD §3.1. |
| R8 | `_pr_comment_mark_addressed` semantics change if author-filter is mis-ordered      | MEDIUM   | TDD §3.3.5 explicitly states `_pr_comment_mark_addressed` continues to record every visible ID. Add an explicit test asserting that bot IDs DO land in `addressed_ids[]`. |
| R9 | Synthetic-readiness inadvertently merges a draft PR                                | LOW      | ADR-002 (TDD §12) gates on `mergeable == MERGEABLE`. Drafts return `mergeable=false` on GitHub. Covered by TDD §7 existing draft-handling tests. |

---

## 8. Quality Gates (apply to every task)

After each task:

1. **Lint:** `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh` returns 0 (no NEW warnings vs main).
2. **Tests:** All bats files in `plugins/autonomous-dev/tests/bats/` return 0.
3. **Config validity:** `jq empty plugins/autonomous-dev/config/defaults.json` returns 0.
4. **No phase-state mutations** in the production code unrelated to the TDD scope.

At plan completion (after TASK-008):

5. **End-to-end:** Run `bats plugins/autonomous-dev/tests/bats/` and confirm zero failures.
6. **Audit trail check:** Manually inspect a generated `merge_decision` event from one of the synthetic-readiness tests to confirm `ignored=[…]` is present and parseable.

---

## 9. Out of Scope (explicitly NOT in this plan)

- Regex / glob matching for `merge_gate_non_blocking_checks` (TDD §11.1 — deferred).
- Batching `gh pr view` calls (TDD §11.2 — optional micro-refactor, executor's call).
- Purging bot IDs from `addressed_ids[]` (TDD §11.3 — declined).
- Prometheus / structured-log counter for synthetic-readiness merges (TDD §11.5 — out of scope).
- Any change to G1/G2/G3/G4 gate ordering or semantics — TDD §3.2.3 explicitly preserves them.
- Any persisted-state schema or event-shape change — TDD §5 explicitly forbids.

---

## 10. Acceptance / Definition of Done for the whole REQ-000054

The request is complete when:

1. All 8 tasks have green CI on their per-task bats commands.
2. The 7 new bats tests in `pr_comment_loop.bats` + the 8 new tests in `trust_gated_merge.bats` + the 2 new tests in `merge_gate_order_aware.bats` all pass.
3. On a real PR in this repo whose only failing checks are `markdown`, `visual-regression`, and/or `scope-enforcement/kind`, `maybe_merge_integration_pr` records a `merged` event with `reason` containing `synthetic-readiness, ignored=[…]` (verified manually on the next integration run).
4. The supervisor stops looping on stale comments: a fresh PR with 0 actionable comments produces zero `pr_comment_reentry` events; a PR with exactly 1 operator line-comment produces exactly 1 re-entry.
5. CHANGELOG.md mentions REQ-000054 with both #628 and #629 references.

---

## 11. Notes for the Executor

- The TDD is unusually prescriptive — it includes near-final bash and jq snippets. Treat them as reference, but do **not** copy verbatim without verifying the surrounding lines still match. Function line numbers (TDD §14) are approximate.
- Each task's bats additions should be appended to existing files, not split into new files, to keep the test harness fingerprint stable.
- The "permanently red" markdown gate is itself one of the things this work is fixing — do not waste time chasing markdown-lint warnings on the CHANGELOG entry.
- If you discover that the actual check names in this repo's `gh pr checks` output differ from the TDD's defaults (R7), update `config/defaults.json` to the real names in TASK-001 rather than introducing a code workaround.
