# Implementation Plan: Self-Healing Pipeline — Detect + Auto-Remediate In-Run Failures

## 1. Plan Metadata

| Field | Value |
|---|---|
| Title | Self-Healing Pipeline — Implementation Plan |
| Request ID | REQ-000056 |
| Linked TDD | `docs/tdd/REQ-000056-req-000056.md` (v1.0, gate passed) |
| Linked PRD | `docs/prd/REQ-000056-req-000056.md` (v1.0, gate passed) |
| Umbrella Issue | #620 (failure modes: #615, #616, #617, #618) |
| Author | Plan Author Agent (pwatsonr@gmail.com) |
| Date | 2026-06-30 |
| Version | 1.0 |
| Target Repo | `/Users/pwatson/codebase/autonomous-dev` |
| Primary Surfaces | `plugins/autonomous-dev/bin/supervisor-loop.sh`, new `bin/lib/self-heal*.sh`, `intake/reviewers/*`, `docs/schemas/events/` |
| Total Estimated Effort | **~62 hours** (≈ 7.75 dev-days, includes 20% buffer) |
| Critical Path Length | **~22 hours** through 6 sequential tasks (T1 → T2 → T3 → T7 → T11 → T12) |
| Parallel Tracks | **3 tracks** post-T2 (Reviewer-Chain, Phase-Lifecycle, Author-Phase telemetry) |

### Scale rationale
This is a genuinely complex change (multiple modules, new schemas, bash + TS surface, 9 failure modes, integration into the supervisor's hottest paths). Full decomposition is warranted per the "scale rigor to task complexity" rule.

---

## 2. Task List

Tasks are atomic, 0.5–4 h each, mapped 1:1 (or 1:N) to TDD §11 tasks. Effort already includes the +20% buffer mandated by the plan-author guidance.

### TASK-001 — Module skeleton, env defaults, dispatch-table lookup, ADR
- **TDD reference:** §4.1, §11 T1, ADR-1..8.
- **Description:** Create `lib/self-heal.sh` with `selfheal_is_enabled`, `_selfheal_table_lookup`, `_selfheal_safe_call`, `selfheal_dispatch` (initially returning 1 when no detectors registered), and all env-default `${VAR:=...}` assignments. Add `docs/architecture/adr-005-self-heal-dispatch.md` summarizing ADR-1..8 from the TDD. Stub `selfheal_dispatch_*` integration-point dispatcher entry points.
- **Files:**
  - CREATE `plugins/autonomous-dev/bin/lib/self-heal.sh`
  - CREATE `docs/architecture/adr-005-self-heal-dispatch.md`
- **Dependencies:** none.
- **Acceptance criteria:**
  1. `source lib/self-heal.sh` succeeds under `set -euo pipefail`.
  2. `selfheal_is_enabled` returns 0 with `AUTONOMOUS_DEV_SELF_HEAL=1`, returns 1 with `=0`.
  3. `_selfheal_table_lookup F1..F9` each return a non-empty `det|event|rem|policy` string.
  4. Unknown mode (e.g., `FX`) returns rc 1.
  5. ADR doc lists all 8 decisions with context/decision/consequences fields.
- **Lint/test commands:**
  - `shellcheck plugins/autonomous-dev/bin/lib/self-heal.sh`
  - `bats plugins/autonomous-dev/tests/bats/self_heal/skeleton.bats` (new, included)
- **Estimated effort:** 4 h (0.5 d + buffer).
- **Track:** Track-A (foundation).
- **Risks:** Low. None.

### TASK-002 — 15 event JSON schemas under `docs/schemas/events/`
- **TDD reference:** §3.1 module layout, §5.3 canonical event shape, §6.2.
- **Description:** Author 15 new JSON Schema (draft-07) files (one per event type listed in §3.1). All inherit the canonical envelope `{schema_version, timestamp, type, request_id, phase, failure_mode?, policy_name?, evidence}` with mode-specific `evidence` sub-schemas drawn from §4.2 / §4.3.
- **Files:** CREATE 15 schemas under `docs/schemas/events/`:
  - `review_gate_loop_detected.schema.json`
  - `reviewer_timeout_repeated.schema.json`
  - `phase_timeout_with_progress.schema.json`
  - `reviewer_error_detected.schema.json`
  - `reviewer_excluded.schema.json`
  - `suspicious_empty_result.schema.json`
  - `suspicious_fast_result.schema.json`
  - `verification_false_negative_detected.schema.json`
  - `verification_false_negative_corrected.schema.json`
  - `novel_failure_detected.schema.json`
  - `state_ledger_drift_detected.schema.json`
  - `ledger_reconciled.schema.json`
  - `phase_budget_extended.schema.json`
  - `phase_requeued_suspicious.schema.json`
  - `self_heal_summary.schema.json`
- **Dependencies:** none (independent of TASK-001 but bundled into T1 of TDD).
- **Acceptance criteria:**
  1. Each schema validates against ajv's meta-schema (`ajv compile`).
  2. Each schema declares `additionalProperties: false` on the evidence object.
  3. `required` includes `schema_version, timestamp, type, request_id, phase`.
- **Lint/test commands:**
  - `npx ajv compile -s docs/schemas/events/*.schema.json --strict=true`
  - `bats plugins/autonomous-dev/tests/bats/self_heal/schema_validation.bats` (skeleton at this point — full test added in TASK-018)
- **Estimated effort:** 3 h.
- **Track:** Track-A (foundation).
- **Risks:** Low. Mitigation: copy a canonical template, vary only `evidence.properties`.

### TASK-003 — `lib/self-heal-state.sh` (atomic state writers)
- **TDD reference:** §4.4, §11 T2.
- **Description:** Implement `selfheal_state_set(state_file, dot_path, json_value)` using tmp+rename atomicity via jq's `setpath`, and `selfheal_state_get(state_file, dot_path)`. Both must be safe under `set -euo pipefail`.
- **Files:** CREATE `plugins/autonomous-dev/bin/lib/self-heal-state.sh`.
- **Dependencies:** TASK-001 (for env defaults + sourcing convention).
- **Acceptance criteria:**
  1. `selfheal_state_set` writes the value at `current_phase_metadata.self_heal.<dot_path>` and leaves all other state.json content byte-identical (snapshot test).
  2. Setting the same path twice with the same value is a no-op net of timestamps (idempotency).
  3. On `jq` failure, tmp file is cleaned up and rc=2.
  4. `selfheal_state_get` returns empty string + rc 0 when path absent.
- **Lint/test commands:**
  - `shellcheck plugins/autonomous-dev/bin/lib/self-heal-state.sh`
  - `bats plugins/autonomous-dev/tests/bats/self_heal/selfheal_state_idempotent.bats`
- **Estimated effort:** 3 h.
- **Track:** Track-A.
- **Risks:** Medium — concurrent state writes vs. supervisor mutations. Mitigation: tmp+rename pattern documented in §10 / TC-06; rely on filesystem-level rename atomicity.

### TASK-004 — `lib/self-heal-events.sh` (emitter + summary)
- **TDD reference:** §4.5, §11 T2.
- **Description:** Implement `selfheal_emit_event(type, ctx_json, [policy])` with `flock`-protected append to `events.jsonl`, optional ajv schema validation gated by `AUTONOMOUS_DEV_SELF_HEAL_VALIDATE_SCHEMA`. Implement `selfheal_emit_summary(request_id, project)` that groups events by type and writes the `self_heal_summary` line. **Visibility-over-purity:** schema violation degrades to `log_warn` but the event is still appended (FR-DETECT-05 / ADR-4).
- **Files:** CREATE `plugins/autonomous-dev/bin/lib/self-heal-events.sh`.
- **Dependencies:** TASK-001, TASK-002 (schemas), TASK-003 (sourcing helpers).
- **Acceptance criteria:**
  1. Event line is valid JSON; appended via `flock` on `events.jsonl.lock`.
  2. With `VALIDATE_SCHEMA=1` and a deliberately malformed payload, event is still appended AND a `log_warn` line is emitted.
  3. With `VALIDATE_SCHEMA=0`, ajv is never invoked (measured by absence of `ajv` in trace).
  4. `selfheal_emit_summary` produces a `self_heal_summary` event with `counts.{detections,remediations_succeeded,remediations_declined}` matrices.
- **Lint/test commands:**
  - `shellcheck plugins/autonomous-dev/bin/lib/self-heal-events.sh`
  - `bats plugins/autonomous-dev/tests/bats/self_heal/selfheal_emit_event.bats`
- **Estimated effort:** 4 h.
- **Track:** Track-A.
- **Risks:** Medium — flock on macOS dev hosts requires util-linux flock or fallback. Mitigation: detect `flock` availability; on absence, use `(set -C; ...)` lock-file fallback documented in the file header.

### TASK-005 — F8 `detect_novel_failure` + `remediate_capture_and_pause`
- **TDD reference:** §4.2 (detect_novel_failure), §4.3 (remediate_capture_and_pause), §11 T3, §9 security table.
- **Description:** Implement F8 detector + universal fallback remediator. The remediator composes the diagnostic bundle, applies the env allow-list `SELFHEAL_ENV_ALLOWLIST`, truncates to ≤ `DIAG_BUNDLE_MAX_BYTES` (1 MiB), optionally files an issue when `gh` is authenticated AND `AUTONOMOUS_DEV_SELF_HEAL_FILE_ISSUES=1`, and calls existing `escalate_to_paused`.
- **Files:**
  - MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh` (add detector + remediator + wire into dispatch table)
  - MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (`advance_phase` integration hook — see TASK-013)
- **Dependencies:** TASK-001, TASK-003, TASK-004.
- **Acceptance criteria:**
  1. Bundle JSON keys = `{schema_version, request_id, phase, detected_failure_mode, supervisor_version, state_json, last_200_session_log_lines, last_50_events, non_secret_env, issue_filed_url}`.
  2. Env capture excludes any var matching `*TOKEN*|*SECRET*|*KEY*|*PASSWORD*` even if it begins with `AUTONOMOUS_DEV_*` (defense-in-depth on top of the allow-list).
  3. Bundle size ≤ 1 MiB (truncate the session-log section first, then the state.json snapshot if still over).
  4. `gh issue create` runs only when both gates pass; failure of `gh` does not propagate.
  5. `escalate_to_paused` called exactly once with `paused_reason: "novel_failure: <mode_id>"`.
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/F8_novel_failure.bats`, `bats .../R_capture_and_pause.bats`.
- **Estimated effort:** 4 h.
- **Track:** Track-B (critical-path).
- **Risks:**
  - **High:** secret leakage via the diagnostic bundle. Mitigation: explicit allow-list + denylist regex (acceptance #2); bats fixture seeds `AUTONOMOUS_DEV_SECRET_TOKEN=leakme` and asserts it is absent.
  - **Medium:** `gh` CLI absent on developer machines. Mitigation: behavior is opt-in via env flag; default-off.

### TASK-006 — F9 `detect_state_ledger_drift` + `remediate_reconcile_ledger`
- **TDD reference:** §4.2 (detect_state_ledger_drift), §4.3 (remediate_reconcile_ledger), §11 T4.
- **Description:** Detect divergence between `state.json.status` and the intake DB's `requests.status` for the request. Remediator calls the existing `sync_intake_db_row` helper. Wire `selfheal_dispatch ledger_check` at the `advance_phase` post-status-write boundary.
- **Files:**
  - MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh` (detector + remediator)
  - MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (one-line hook — bundled into TASK-013)
- **Dependencies:** TASK-001, TASK-003, TASK-004.
- **Acceptance criteria:**
  1. Drift detected → exactly one `sync_intake_db_row` call.
  2. Drift detected → `state_ledger_drift_detected` event + `ledger_reconciled` event in order.
  3. `sync_intake_db_row` failure → rc=2 from remediator → `R_CAPTURE_AND_PAUSE` invoked.
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/F9_state_ledger_drift.bats`.
- **Estimated effort:** 2 h.
- **Track:** Track-B.
- **Risks:** Low. Mitigation: reuse existing helper; only new code is the diff check.

### TASK-007 — F3 `detect_phase_timeout_with_progress` + `remediate_extend_phase_budget`
- **TDD reference:** §4.2 (F3), §4.3 (R_EXTEND_PHASE_BUDGET), §11 T5, §4.6 row 5.
- **Description:** Detector fires when `working_tree_advanced pre post` returns 0 AND the hard-timeout path triggered. Remediator extends timeout by `BUDGET_EXTENSION_FACTOR` (1.5×), resets `phase_started_at`, persists `budget_extended_*` flags. Modify `resolve_phase_timeout` in `typed-limits.sh` to honor `budget_extended_to`.
- **Files:**
  - MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh`
  - MODIFY `plugins/autonomous-dev/bin/lib/typed-limits.sh` (`resolve_phase_timeout` override hook)
  - MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (`check_phase_timeout` integration — bundled with TASK-013)
- **Dependencies:** TASK-001, TASK-003, TASK-004.
- **Acceptance criteria:**
  1. F3 fires only when both conditions hold (table-driven bats fixtures cover all 4 combinations).
  2. Remediation extends timeout exactly once per (request, phase); a second hard-timeout uses the legacy path.
  3. `phase_budget_extended` event includes `{from, to, factor}`.
  4. `resolve_phase_timeout` returns the extended value while `budget_extended_at` is set, original otherwise.
- **Lint/test commands:** `bats .../F3_phase_timeout_with_progress.bats`, `bats .../R_extend_phase_budget.bats`, `shellcheck plugins/autonomous-dev/bin/lib/typed-limits.sh`.
- **Estimated effort:** 4 h.
- **Track:** Track-C (parallel).
- **Risks:** Medium — modifying `typed-limits.sh` could affect REQ-000051 soft-timeout. Mitigation: kill switch test (TASK-017) compares legacy behavior with `SELF_HEAL=0` against pinned fixtures.

### TASK-008 — F7 `detect_verification_false_negative` + `remediate_self_verify` (incl. stale-artifact guard)
- **TDD reference:** §4.2 (F7), §4.3 (R_SELF_VERIFY), §11 T6, ADR-5, §9 Risk #2.
- **Description:** Detect when phase reported `fail` but the referenced test-results artifact shows 0 failures AND `mtime ≥ phase_started_at`. Remediator atomically rewrites `result_file` setting `status=pass`, `self_verified=true`, `original_status=fail`. Modify `advance_phase` to bypass the escalation_count++ branch when `self_verified == true`.
- **Files:**
  - MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh`
  - MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (`advance_phase` self_verified bypass — bundled with TASK-013)
- **Dependencies:** TASK-001, TASK-003, TASK-004.
- **Acceptance criteria:**
  1. Stale artifact (mtime < `phase_started_at`) → no override; `self_verify_skipped_stale_artifact` event emitted.
  2. Missing artifact → no override; emits `self_verify_skipped_missing_artifact`.
  3. Fresh artifact, 0 failures → result_file rewritten, retry_count unchanged across two `advance_phase` calls.
  4. Idempotent: running remediator twice on same result_file mutates exactly once.
- **Lint/test commands:** `bats .../F7_verification_false_negative.bats`, `bats .../F7_stale_artifact.bats`, `bats .../R_self_verify.bats`.
- **Estimated effort:** 5 h.
- **Track:** Track-C (parallel).
- **Risks:**
  - **High:** masking a real failure. Mitigation: stale-artifact guard + emit event every cycle so operators see the pattern; ADR-5 explicitly documents single-pass policy.

### TASK-009 — F4 detector + `remediate_retry_then_exclude` (bash side)
- **TDD reference:** §4.2 (F4), §4.3 (R for F4), §11 T7 (bash portion).
- **Description:** Implement F4 detector (recognizes `REVIEW_GATE_CLI_NONZERO | REVIEW_GATE_BAD_JSON | REVIEW_GATE_RESULT_WRITE_FAILED`). Remediator implements the retry-then-exclude state machine in bash: first hit → set `reviewer_retry[<name>] = 1` and signal caller to redispatch; second hit → consult chain config `blocking` field, either fail-blocking or add to `excluded_reviewers` and redispatch.
- **Files:** MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh`.
- **Dependencies:** TASK-001, TASK-003, TASK-004.
- **Acceptance criteria:**
  1. Detector returns 0 for the three status_reasons listed; returns 1 otherwise.
  2. First detection → `reviewer_retry_scheduled` event + rc=0.
  3. Second detection with `blocking=false` → `reviewer_excluded` event + rc=0.
  4. Second detection with `blocking=true` → `reviewer_failed_blocking` event + rc=1.
  5. Missing `blocking` field in chain config → assumed `blocking=true` with `reviewer_blocking_default_assumed` event (§7.1).
- **Lint/test commands:** `bats .../F4_reviewer_error_detected.bats`, `bats .../R_retry_once_then_exclude.bats`.
- **Estimated effort:** 4 h.
- **Track:** Track-B (critical-path; gates TASK-011/012).
- **Risks:** Medium — coupling to chain-config schema. Mitigation: chain-config schema update done in TASK-010.

### TASK-010 — TS surface: `ReviewerSpec.blocking`, `ReviewerOutcome.errorKind`, runner exclude/retry, CLI `--state-file`
- **TDD reference:** §4.7, §11 T7 (TS portion), OQ-1/ADR-7.
- **Description:** Update TypeScript types and the runner to honor the per-reviewer policy. Add `--state-file` flag to `bin/review-gate-cli.ts` that reads `excluded_reviewers` and `reviewer_timeout_overrides` from state.json and threads them into `runReviewers`. Update chain config schema with optional `blocking: boolean` (default `true`).
- **Files:**
  - MODIFY `plugins/autonomous-dev/intake/reviewers/types.ts`
  - MODIFY `plugins/autonomous-dev/intake/reviewers/runner.ts`
  - MODIFY `plugins/autonomous-dev/intake/reviewers/review-gate-orchestrator.ts`
  - MODIFY `plugins/autonomous-dev/bin/review-gate-cli.ts`
  - MODIFY existing `plugins/autonomous-dev/intake/reviewers/chains/*.json` to add `blocking: true` explicitly to all current entries
  - CREATE Jest specs in `plugins/autonomous-dev/intake/reviewers/__tests__/`
- **Dependencies:** TASK-009 (so contract is finalized before TS lands).
- **Acceptance criteria:**
  1. `runReviewers({excludedReviewers: ['X']})` never invokes the invoker for reviewer X.
  2. `runReviewers({retryOnce: true})` retries an ERROR result exactly once per reviewer per gate.
  3. `review-gate-cli --state-file <path>` reads the two self-heal fields and applies them.
  4. `ReviewerOutcome.errorKind` populated for `reviewer_timeout | bad_json | cli_nonzero`.
  5. All existing chain configs include explicit `blocking: true` entries.
- **Lint/test commands:**
  - `npm --prefix plugins/autonomous-dev test -- runner.retry-then-exclude.test.ts`
  - `npm --prefix plugins/autonomous-dev test -- runner.excluded-reviewers.test.ts`
  - `npm --prefix plugins/autonomous-dev test -- review-gate-cli.state-file.test.ts`
  - `npm --prefix plugins/autonomous-dev run lint`
  - `npm --prefix plugins/autonomous-dev run typecheck`
- **Estimated effort:** 4 h.
- **Track:** Track-B.
- **Risks:** Medium — cross-cutting type changes; existing call sites may break. Mitigation: typecheck + Jest + bats e2e in TASK-019 cover the seam.

### TASK-011 — F1 `detect_review_gate_loop` + `remediate_fall_back_to_single_reviewer`
- **TDD reference:** §4.2 (F1), §4.3 (R_FALL_BACK_TO_SINGLE_REVIEWER), §11 T8.
- **Description:** Detect ≥3 consecutive REQUEST_CHANGES outcomes from the same reviewer with stable fingerprint (sha256 of first 256 bytes of reason). Remediator sets `review_chain_disabled=true`. Patch `should_use_review_chain` to consult the flag.
- **Files:**
  - MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh`
  - MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (`should_use_review_chain` flag check — bundled with TASK-013)
- **Dependencies:** TASK-009 (review_outcome integration point), TASK-010 (perReviewerOutcomes shape).
- **Acceptance criteria:**
  1. Same reviewer + same fingerprint × 3 → detector fires.
  2. Different reviewer or changing fingerprint resets counter to 1.
  3. After remediation, `should_use_review_chain` returns 1 (false) for that request.
  4. `review_chain_disabled` survives across phase advances within the same request (preserved in `advance_phase` reset list — see TASK-013).
- **Lint/test commands:** `bats .../F1_review_gate_loop.bats`.
- **Estimated effort:** 3 h.
- **Track:** Track-B.
- **Risks:** Low. Mitigation: fingerprint algorithm documented in OQ-S1; future spec phase can refine.

### TASK-012 — F2 `detect_reviewer_timeout_repeated` + `remediate_escalate_reviewer_timeout`
- **TDD reference:** §4.2 (F2), §4.3 (R_ESCALATE_REVIEWER_TIMEOUT), §11 T9.
- **Description:** Detect repeated reviewer-timeout `errorKind` for a single reviewer; threshold 2 by default. Remediator on first hit doubles the per-reviewer timeout (via `reviewer_timeout_overrides`); on second hit (already extended), falls through to R_FALL_BACK_TO_SINGLE_REVIEWER.
- **Files:** MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh`.
- **Dependencies:** TASK-009, TASK-010, TASK-011 (uses R_FALL_BACK as fallback).
- **Acceptance criteria:**
  1. Threshold-1 case → no event.
  2. Threshold case → `reviewer_timeout_extended` event + override persisted.
  3. Already-extended case → fallthrough → `review_chain_disabled=true`.
  4. `review-gate-cli` consumes the override on the next invocation (verified via TS test in TASK-010 stub + e2e in TASK-019).
- **Lint/test commands:** `bats .../F2_reviewer_timeout_repeated.bats`.
- **Estimated effort:** 3 h.
- **Track:** Track-B.
- **Risks:** Low.

### TASK-013 — Supervisor-loop integration (8 hook sites + preserve list)
- **TDD reference:** §4.6 modification table, §11 T3/T4/T5/T6/T8.
- **Description:** Apply all 8 documented modifications to `supervisor-loop.sh` in one focused diff (so the kill-switch regression test in TASK-017 can pin one boundary). Also extend the `advance_phase` reset list to preserve `excluded_reviewers` and `review_chain_disabled` across phase advances (ADR-2). Source `self-heal.sh` near `typed-limits.sh`.
- **Files:** MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh`.
- **Dependencies:** TASK-005, TASK-006, TASK-007, TASK-008, TASK-009, TASK-011, TASK-012 (every hook needs its detector/remediator merged first).
- **Acceptance criteria:**
  1. All 8 hooks land at the documented call sites (table §4.6).
  2. Each hook is wrapped in `if selfheal_is_enabled; then ... fi`.
  3. `advance_phase` preserve-list keeps `excluded_reviewers` and `review_chain_disabled` across phase transitions.
  4. `shellcheck` clean.
  5. With `AUTONOMOUS_DEV_SELF_HEAL=0`, the supervisor-loop control flow is bit-for-bit identical to pre-change (verified by TASK-017).
- **Lint/test commands:**
  - `shellcheck plugins/autonomous-dev/bin/supervisor-loop.sh`
  - `bats plugins/autonomous-dev/tests/bats/supervisor/*.bats` (existing — must remain green)
- **Estimated effort:** 4 h.
- **Track:** Track-B (critical-path merge point).
- **Risks:**
  - **High:** integrating into the supervisor's hottest paths can introduce regressions. Mitigation: kill-switch test (TASK-017) pins legacy behavior; each hook is conditioned on `selfheal_is_enabled`.
  - **Medium:** merge contention with concurrent work on `supervisor-loop.sh`. Mitigation: schedule TASK-013 as a single-track gate; no other parallel task touches this file.

### TASK-014 — F5/F6 `detect_suspicious_*` + `remediate_requeue_author_phase_once` (incl. prompt hint)
- **TDD reference:** §4.2 (F5/F6), §4.3 (R_REQUEUE_AUTHOR_PHASE_ONCE), §11 T10.
- **Description:** Detect suspicious-empty (pass + 0 artifacts + < threshold sec) and suspicious-fast (< baseline / ratio). Remediator moves result_file aside, resets state for re-run, sets `suspicious_previous_result=true`. Modify `resolve_phase_prompt` in `phase-helpers.sh` to prepend a hint paragraph when the flag is set.
- **Files:**
  - MODIFY `plugins/autonomous-dev/bin/lib/self-heal.sh`
  - MODIFY `plugins/autonomous-dev/bin/lib/phase-helpers.sh` (`resolve_phase_prompt` hint)
- **Dependencies:** TASK-001, TASK-003, TASK-004, TASK-013 (session_outcome hook lands there).
- **Acceptance criteria:**
  1. F5 fires for pass+0-artifacts+<5s; not for pass+1-artifact.
  2. F6 returns 1 when no baseline available (cannot judge).
  3. Remediator allowed once per phase; second invocation falls through to R_CAPTURE_AND_PAUSE.
  4. Backed-up result file matches `phase-result-<phase>.json.suspicious-*.bak` glob.
  5. Prompt hint appears in next session's prompt input (verified via prompt-resolution unit bats).
- **Lint/test commands:** `bats .../F5_suspicious_empty.bats`, `bats .../F6_suspicious_fast.bats`, `bats .../R_requeue_author_phase_once.bats`.
- **Estimated effort:** 4 h.
- **Track:** Track-D (parallel after TASK-013).
- **Risks:** Medium — false positives may trigger spurious requeues. Mitigation: F6 conservatively returns 1 with no baseline; F5 thresholds env-overridable.

### TASK-015 — Phase-duration median telemetry collector
- **TDD reference:** §4.2 (F6), §6.1, §11 T10.
- **Description:** Implement a lightweight per-session hook (in `dispatch_phase_session` return path) that updates `${DAEMON_HOME}/self-heal/phase-duration-medians.json` with the latest observed duration. Use a streaming median approximation (Greenwald-Khanna or simple bounded ring buffer + sort) to keep file size bounded.
- **Files:**
  - CREATE `plugins/autonomous-dev/bin/lib/self-heal-telemetry.sh`
  - MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (one-line hook adjacent to existing TASK-013 hook)
- **Dependencies:** TASK-013 (hook site already prepared).
- **Acceptance criteria:**
  1. File created on first session; size stays ≤ 64 KiB across 1000 simulated sessions.
  2. Median for (phase) lookup returns plausible value (within 20 % of true median in bats fixture).
  3. Concurrent updates are flock-protected.
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/telemetry_medians.bats`.
- **Estimated effort:** 3 h.
- **Track:** Track-D.
- **Risks:** Low. Mitigation: best-effort; F6 detector treats missing baseline as not-detected.

### TASK-016 — `selfheal_emit_summary` integration in `advance_phase` terminal branches
- **TDD reference:** §4.5, §11 T11.
- **Description:** Wire `selfheal_emit_summary` into the three terminal branches of `advance_phase` (done / paused / failed) before the existing log line. Honors the "emit once per terminal entry" stance (OQ-S2).
- **Files:** MODIFY `plugins/autonomous-dev/bin/supervisor-loop.sh` (3 small additions).
- **Dependencies:** TASK-013 (sourcing in place), TASK-004 (`selfheal_emit_summary`).
- **Acceptance criteria:**
  1. Each terminal transition produces exactly one `self_heal_summary` event.
  2. `counts.detections / remediations_succeeded / remediations_declined` matrices align with the actual event log.
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/selfheal_summary_emitted.bats`.
- **Estimated effort:** 2 h.
- **Track:** Track-B.
- **Risks:** Low.

### TASK-017 — Kill-switch regression against pinned fixtures
- **TDD reference:** §8.1 (`kill_switch_disabled.bats`), §11 T11, NFR-COMPAT-02, ADR-6.
- **Description:** Build a comprehensive bats suite that runs the supervisor against a deterministic fixture (phase result + state.json) twice — once with `AUTONOMOUS_DEV_SELF_HEAL=0`, once with the same env but without self-heal sourced (the pre-change baseline captured as a golden artifact). Both runs MUST produce byte-identical state.json + events.jsonl outputs.
- **Files:**
  - CREATE `plugins/autonomous-dev/tests/bats/self_heal/kill_switch_disabled.bats`
  - CREATE `plugins/autonomous-dev/tests/bats/self_heal/fixtures/kill_switch/*` (golden snapshots)
- **Dependencies:** TASK-013, TASK-016 (full integration landed).
- **Acceptance criteria:**
  1. `diff` of post-run `state.json` between baseline and `SELF_HEAL=0` paths is empty.
  2. `diff` of `events.jsonl` is empty.
  3. No `self_heal_*` events present in the `SELF_HEAL=0` run.
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/kill_switch_disabled.bats`.
- **Estimated effort:** 3 h.
- **Track:** Track-B.
- **Risks:** Medium — golden snapshots can be brittle. Mitigation: normalize timestamps via post-processing `jq` filter prior to diff.

### TASK-018 — Schema-validation regression bats
- **TDD reference:** §8.1 (`schema_validation.bats`), NFR-OBSERVABILITY-01.
- **Description:** Emit one synthetic event per schema and assert ajv-validation passes. Then mutate a required field and assert validation fails (so ajv is actually checking, not no-op).
- **Files:** CREATE `plugins/autonomous-dev/tests/bats/self_heal/schema_validation.bats`.
- **Dependencies:** TASK-002, TASK-004.
- **Acceptance criteria:**
  1. 15 happy-path validations pass.
  2. 15 mutation-validations fail.
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/schema_validation.bats`.
- **Estimated effort:** 2 h.
- **Track:** Track-A.
- **Risks:** Low.

### TASK-019 — End-to-end bats: transient reviewer flake + novel-failure resume
- **TDD reference:** §8.1 e2e rows, §11 T12.
- **Description:** Two e2e suites that drive a real (but isolated) supervisor run end-to-end:
  - `e2e_transient_reviewer_flake.bats` — reviewer returns ERROR once, then APPROVE; assert request completes WITHOUT pause.
  - `e2e_novel_failure_resume.bats` — synthesize an unknown status; assert paused with `novel_failure`; then `autonomous-dev request resume <id>` succeeds.
- **Files:** CREATE the two bats files under `plugins/autonomous-dev/tests/bats/self_heal/`.
- **Dependencies:** TASK-013 through TASK-017.
- **Acceptance criteria:**
  1. Both suites green locally and in CI.
  2. Each exercises at least one full supervisor cycle (no mocked dispatch).
- **Lint/test commands:** `bats plugins/autonomous-dev/tests/bats/self_heal/e2e_*.bats`.
- **Estimated effort:** 4 h.
- **Track:** Track-B (penultimate critical-path).
- **Risks:** Medium — e2e fragility. Mitigation: use the existing supervisor test harness; isolate via `AUTONOMOUS_DEV_STATE_DIR` overrides.

### TASK-020 — CI wiring + operator docs + migration note
- **TDD reference:** §11 T12, §14.
- **Description:** Add `tests/bats/self_heal/` to the CI workflow file. Author operator documentation: env vars (`AUTONOMOUS_DEV_SELF_HEAL*` listing, defaults, semantics), how to read `self_heal_summary`, how to resume a `novel_failure` paused request, the kill-switch escape hatch for operators preferring legacy fast-fail. Update CHANGELOG / release notes.
- **Files:**
  - MODIFY `.github/workflows/<bats-or-ci>.yml` (workflow path TBD — verify before editing)
  - CREATE `docs/operations/self-healing-pipeline.md`
  - MODIFY `docs/operations/configuration.md` (or equivalent) to add env-var section
  - MODIFY `CHANGELOG.md`
- **Dependencies:** TASK-019 (e2e green before docs ship).
- **Acceptance criteria:**
  1. CI workflow runs the new bats dir on pull requests.
  2. Operator doc covers all 9 failure modes + their remediations + their env knobs.
  3. Migration note explicitly tells "fast-fail" operators to set `AUTONOMOUS_DEV_SELF_HEAL=0`.
- **Lint/test commands:** `markdownlint docs/operations/self-healing-pipeline.md`, CI bats run on PR.
- **Estimated effort:** 3 h.
- **Track:** Track-B (final task).
- **Risks:** Low.

---

## 3. Dependency Graph

```
TASK-001 (skeleton)
  ├─► TASK-002 (schemas)                ──┐
  ├─► TASK-003 (state writers)           ─┤
  │     └─► TASK-004 (events emitter)  ──┤
  │           ├─► TASK-005 (F8 + R_capture)*  ──┐
  │           ├─► TASK-006 (F9 ledger)         │
  │           ├─► TASK-007 (F3 budget)         │
  │           ├─► TASK-008 (F7 self-verify)    │
  │           ├─► TASK-009 (F4 retry/exclude)* ─┤
  │           │     └─► TASK-010 (TS surface)* ─┤
  │           │           ├─► TASK-011 (F1)    │
  │           │           └─► TASK-012 (F2)    │
  │           ├─► TASK-014 (F5/F6 requeue)   ──┤
  │           └─► TASK-018 (schema-validation bats)
  │
  └─► (all of T1 funnels into) TASK-013 (supervisor-loop integration)*  ◄── merge gate
        ├─► TASK-015 (telemetry collector)
        ├─► TASK-016 (summary emitter integration)*
        │     └─► TASK-017 (kill-switch regression)*
        │           └─► TASK-019 (e2e bats)*
        │                 └─► TASK-020 (CI + docs)*

* = critical path (longest sequential chain)
```

### Critical path (sequential)
TASK-001 → TASK-003 → TASK-004 → TASK-009 → TASK-010 → TASK-013 → TASK-016 → TASK-017 → TASK-019 → TASK-020

**Critical-path effort:** 4 + 3 + 4 + 4 + 4 + 4 + 2 + 3 + 4 + 3 = **35 h** (≈ 4.4 dev-days at 8 h/day).

(Note: critical-path metric quoted in §1 metadata = ~22 h refers to the chain length AFTER TASK-001/002/003/004 foundation; total wall-clock with no parallelism is 35 h.)

### Cycle check
None. Graph is a DAG.

---

## 4. Parallel Execution Schedule

Once TASK-004 lands, three tracks can proceed concurrently:

| Wave | Track-A (Foundation) | Track-B (Reviewer-chain / Critical) | Track-C (Phase-lifecycle) | Track-D (Author-phase telemetry) |
|---|---|---|---|---|
| W1 | TASK-001, TASK-002 | — | — | — |
| W2 | TASK-003 | — | — | — |
| W3 | TASK-004, TASK-018 | — | — | — |
| W4 | — | TASK-005, TASK-009 | TASK-006, TASK-007, TASK-008 | TASK-014 |
| W5 | — | TASK-010 | — | TASK-015 (prep) |
| W6 | — | TASK-011, TASK-012 | — | — |
| W7 | — | **TASK-013 (merge gate)** | — | — |
| W8 | — | TASK-015, TASK-016 | — | — |
| W9 | — | TASK-017 | — | — |
| W10 | — | TASK-019 | — | — |
| W11 | — | TASK-020 | — | — |

### File-conflict matrix
- `supervisor-loop.sh` is touched only by TASK-013, TASK-015, TASK-016 — all sequential on Track-B.
- `lib/self-heal.sh` is touched by many tasks; the dispatch-table additions are append-only per mode, so merge conflicts are mechanical (per-mode `case` rows).
- Track-C and Track-D never touch `supervisor-loop.sh` until they hand off into Track-B's TASK-013.

### Wall-clock estimate with parallelism
With three concurrent executors and the critical path on Track-B: **~4.5 dev-days** (matches TDD §11 estimate).

---

## 5. Effort Summary

| Task | Hours |
|---|---|
| TASK-001 | 4 |
| TASK-002 | 3 |
| TASK-003 | 3 |
| TASK-004 | 4 |
| TASK-005 | 4 |
| TASK-006 | 2 |
| TASK-007 | 4 |
| TASK-008 | 5 |
| TASK-009 | 4 |
| TASK-010 | 4 |
| TASK-011 | 3 |
| TASK-012 | 3 |
| TASK-013 | 4 |
| TASK-014 | 4 |
| TASK-015 | 3 |
| TASK-016 | 2 |
| TASK-017 | 3 |
| TASK-018 | 2 |
| TASK-019 | 4 |
| TASK-020 | 3 |
| **Total** | **62 h** |

Includes 20 % buffer per plan-author guidance. Test writing accounts for ~40 % of each task's estimate.

---

## 6. Risk Register

| ID | Risk | Severity | Mitigation | Owning task(s) |
|---|---|---|---|---|
| RISK-01 | Diagnostic bundle leaks secrets via env vars or session log | High | Explicit allow-list + denylist regex; bats fixture asserts secret string absent | TASK-005 |
| RISK-02 | F7 self-verify masks a real intermittent test failure | High | Mtime ≥ phase_started_at guard; emit event every cycle for pattern visibility; ADR-5 single-pass policy | TASK-008 |
| RISK-03 | Supervisor-loop integration regresses non-self-heal paths | High | Every hook wrapped in `selfheal_is_enabled`; kill-switch regression compares against pinned fixtures | TASK-013, TASK-017 |
| RISK-04 | `resolve_phase_timeout` change interferes with REQ-000051 soft-timeout | Medium | Behavior gated by `budget_extended_at` presence; kill-switch test exercises legacy path | TASK-007 |
| RISK-05 | `flock` unavailable on macOS dev environment | Medium | Detect availability; fall back to `set -C` lock-file pattern documented in module header | TASK-004 |
| RISK-06 | E2E bats fragility under CI runners | Medium | Use existing harness; isolate via env overrides; normalize timestamps before diff | TASK-019 |
| RISK-07 | Merge contention on `supervisor-loop.sh` | Medium | All `supervisor-loop.sh` edits restricted to Track-B sequential tasks (TASK-013/015/016) | scheduling |
| RISK-08 | Chain-config schema bump breaks consumers without `blocking` field | Low | Conservative default `blocking: true` (gate-blocking matches current behavior); `reviewer_blocking_default_assumed` event for visibility | TASK-009, TASK-010 |
| RISK-09 | F1 fingerprint algorithm too brittle / too lenient | Low | Algorithm documented as OQ-S1; spec phase can tune; threshold env-overridable | TASK-011 |
| RISK-10 | Schema files balloon CI install time via ajv install | Low | ajv already in repo dev deps (assumed); confirm during TASK-018 | TASK-002, TASK-018 |
| RISK-11 | Backward-compat regressions in event consumers (portal) | Low | TDD §14 explicitly notes portal already handles unknown event types via default-noop | TASK-002, TASK-016 |

### Open clarification items propagated from TDD
- **OQ-S1** (F1 fingerprint normalization) — surfaces to spec phase; current plan uses sha256(first 256B) as documented in TDD §13.
- **OQ-S2** (summary dedup across pause→done resumes) — current plan emits once per terminal entry; portal sums.
- **OQ-3** (per-(phase,request_type) baseline) — deferred; v1 uses per-phase median across all request types.

No blocking findings against the TDD. Plan is executable as-written.

---

## 7. Traceability Index

| PRD AC / FR / NFR | Plan task(s) |
|---|---|
| FR-DETECT-01 (reviewer timeout repeated) | TASK-012 |
| FR-DETECT-02 (reviewer error / phase timeout w/ progress) | TASK-007, TASK-009 |
| FR-DETECT-05 (events always emitted) | TASK-004 |
| FR-DETECT-06 (review-gate-loop detection) | TASK-011 |
| FR-DETECT-07 (suspicious empty / fast) | TASK-014 |
| FR-REM-01 (idempotent remediators) | TASK-003, TASK-005..TASK-014 |
| FR-REM-02 (budget extended once) | TASK-007 |
| FR-REM-03 (retry-once-then-exclude) | TASK-009, TASK-010 |
| FR-REM-04 (self-verify) | TASK-008 |
| FR-REM-05 (requeue once) | TASK-014 |
| FR-REM-06 (capture + pause + bundle) | TASK-005 |
| FR-REM-07 (ledger reconcile) | TASK-006 |
| FR-CFG-02 (kill switch) | TASK-001, TASK-017 |
| FR-OBSERVE (event schemas) | TASK-002, TASK-004, TASK-018 |
| FR-TEST-01..04 (per-mode + e2e coverage) | TASK-018, TASK-019, individual F-task bats |
| NFR-COMPAT-01 (no state migrations) | TASK-003 (additive paths only) |
| NFR-COMPAT-02 (bit-for-bit legacy when disabled) | TASK-013, TASK-017 |
| NFR-SECURITY-01 (env allow-list, no secret leakage) | TASK-005 |
| NFR-PERF-01 (≤50 ms detection overhead) | TASK-001 (lazy short-circuit), TASK-005 |
| NFR-PERF-02 (≤500 ms diag bundle) | TASK-005 |
| NFR-MAINTAINABILITY-01 (new mode in ≤3 files) | TASK-001 (table design), all F-tasks |
| NFR-OBSERVABILITY-01 (schema validation) | TASK-018 |
| S-OP-1 (transient flake) | TASK-019 e2e |
| S-OP-2 (self-verify) | TASK-008 |
| S-US-1 (novel-failure resume) | TASK-019 e2e |
| ADR-1..8 | TASK-001 (ADR doc), respective F-tasks |

Every TDD design element maps to ≥1 task.

---

## 8. Definition of Done (plan-level)

The plan is complete when:
1. All 20 tasks pass their acceptance criteria.
2. CI runs the new `tests/bats/self_heal/` directory green on every PR.
3. Kill-switch regression (TASK-017) diff is empty.
4. Operator docs (TASK-020) merged.
5. ADR-005 merged in `docs/architecture/`.
6. No regression in existing bats suites under `tests/bats/`.
7. PR linked to issue #620 and references closing #615, #616, #617, #618.

*End of Plan — REQ-000056 v1.0.*
